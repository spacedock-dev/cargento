---
title: "B2g(build) · Pi: report Needs input from ui_prompt_start / ui_prompt_end"
linear-status: In Progress
milestone: Don't be the bottleneck
release: "later"
move: "sharpen"
promise: "P3"
estimate: "L"
source: "https://linear.app/recce/issue/DRC-4380/b2gbuild-pi-report-needs-input-from-ui-prompt-start-ui-prompt-end"
status: triage
score: 0.2
started: 2026-09-15T03:53:02Z
completed: ""
verdict: ""
worktree: ""
issue: ""
pr: ""
mod-block: ""
reconciled: ""
gates:
    version: 1
    records:
        - id: gate:drc-4380:triage
          stage: triage
          attempts:
            - id: gate-attempt:drc-4380-triage-1
              briefing:
                id: briefing:drc-4380:triage:attempt-1:revision-1
                digest: sha256:d8bda3d71df21725321a7ca4e5b250d1600a7fdae849b96ea63cb8d0333965a1
                room-ref: ./drc-4380/review/triage/briefing-1
              resolution:
                type: Resolution
                id: resolution:spacedock:drc-4380:triage:1
                briefing: briefing:drc-4380:triage:attempt-1:revision-1
                by: person:captain
                at: "2026-09-15T04:10:42.249671Z"
                decision: approve
                reason: 'Captain: I approve these boundaries and drafts. Accepts measured Pi 0.85.1 extension prompts in persisted sessions, explicit startup-trust and pre-persistence exclusions, eight acceptance criteria and separate surface budgets.'
              application:
                target-stage: implementation
                state: pending
---

[DRC-4380](https://linear.app/recce/issue/DRC-4380/b2gbuild-pi-report-needs-input-from-ui-prompt-start-ui-prompt-end) — B2g(build) · Pi: report Needs input from ui_prompt_start / ui_prompt_end

Seeded from the full Linear project read on 2026-09-15. Linear owns the current issue body and blocking relations; selection must fetch them before choosing work. No triage, approval, implementation, or delivery is claimed.

## Linear edits made

No issue or milestone description was edited. FO separately moved the issue to In Progress. Originals below were fetched live on 2026-09-15; their text is preserved verbatim.

### Original issue body — DRC-4380 — 2026-09-15

## User value

A person running a Pi session notices this when a standing prompt puts that session in the gate queue instead of leaving the board unable to say either way. P3, sharpen: gate detection reaches Pi, on a documented event pair rather than an inferred one.

## Why this exists, and what it replaces

[DRC-4190](<https://linear.app/recce/issue/DRC-4190/b2g-pi-does-anything-fire-while-pis-own-approval-prompt-stands>) (B2g) asked "does anything fire while Pi's own approval prompt stands?" **Pi has no approval prompt**, so that question has no subject and the issue is closed as refuted at its premise. This issue is the question that survives it.

Pi's own documentation settles both halves:

* **No built-in per-tool gate.** `project_trust` is the only standing prompt Pi raises on its own, it fires once per directory rather than per tool call, and the docs are explicit that it "is only an input-loading guard. It prevents a repository from silently changing pi's settings or extensions before you approve it. It does not make untrusted code, untrusted prompts, or untrusted model output safe." An approval gate on Pi is something an extension implements through `tool_call` returning `{ block: true }`, which is what the third-party forks do.
* **But Pi documents a waiting-for-user signal, and it is the cleanest on the board after OpenCode's.** `ui_prompt_start` and `ui_prompt_end` fire around `ctx.ui.select()`, `ctx.ui.confirm()`, `ctx.ui.input()`, `ctx.ui.editor()` and `ctx.ui.custom()`, and the docs say why in the vendor's own words: so that "host/status integrations can report 'waiting for user' instead of just 'running'". `ctx.sessionManager.getSessionId()` supplies the key.

So Pi's registry row is correctly `False` today, and the reason on the board should be "we ship no adapter", not "no signal exists".

## What needs to be done

Route 1, a bundled adapter, the same class as the OpenCode build:

* A TypeScript extension bundled under `cargento/skills/cargento/` for `~/.pi/agent/extensions/`, subscribing to `ui_prompt_start` and `ui_prompt_end` and posting them with `getSessionId()`.
* `EVENTS_BY_HARNESS` gains the Pi table mapping the pair to `input_requested` and `input_resolved`.
* `IDENTITY_NORMALIZERS["pi"]`, keyed to whatever `collectors/pi.py` already keys on.
* Flip the registry row, proved by <issue id="e8ccc627-2344-44b4-b51f-1aa30ab27b7c" href="https://linear.app/recce/issue/DRC-4378/restore-the-reports-needs-input-oracles-and-the-comment-that-claims">DRC-4378</issue>'s restored derivation.

## What must be measured before it ships

`ui_prompt_start` is documented and unmeasured here. **Pi is not installed on this machine** and no loopback stand-in exists in this repository, so a capture costs a contained install plus a stdlib OpenAI-compatible server before the first datum. Record to `docs/captures/pi/`, and do not flip the flag on documentation alone.

The pair fires around **any** UI prompt, which on stock Pi means `project_trust` and nothing else. Whether a once-per-directory trust prompt should raise a gate row, or be filtered, is part of this issue.

## Acceptance criteria

* **User-visible:** a Pi session held at a prompt appears in the gate queue as Needs input, and clears when it is answered. **Verified by:** drive one Pi session to a prompt with the extension installed.
* The flip is proved by derivation rather than declared. **Verified by:** <issue id="e8ccc627-2344-44b4-b51f-1aa30ab27b7c" href="https://linear.app/recce/issue/DRC-4378/restore-the-reports-needs-input-oracles-and-the-comment-that-claims">DRC-4378</issue>'s oracle.
* A capture under `docs/captures/pi/` carries the measured event shape, not the documented one.

### Original milestone description — 2026-09-15

## The user value

**One queue of everything blocked on you, with what it is waiting for and how long it has waited. A session can also ask you a question directly and wait for your answer.**

## What is left

[DRC-4379](<https://linear.app/recce/issue/DRC-4379>): See and clear OpenCode permission waits in the gate queue.

[DRC-4380](<https://linear.app/recce/issue/DRC-4380>): See and clear Pi prompts in the gate queue, after measuring the event pair.

[DRC-4208](<https://linear.app/recce/issue/DRC-4208>): Route Droid's hook events to the correct harness rather than silently losing them.

[DRC-4203](<https://linear.app/recce/issue/DRC-4203>): Keep a standing wait visible through ordinary heartbeats, with an explicit expiry.

[DRC-4200](<https://linear.app/recce/issue/DRC-4200>): Keep the captures supporting gate detection within their privacy contract.

[DRC-4199](<https://linear.app/recce/issue/DRC-4199>): Keep nearly simultaneous waits in a stable, truthful queue order.

[DRC-4194](<https://linear.app/recce/issue/DRC-4194>): Base the Pi gate-detection scoring audit on an actual source.

## Waits on

No open blocking relation is recorded on these issues.

## One question left open, gating nothing

Whether a raise survives the terminal that launched it. Cargento ships the socket case alone, so nothing that shipped depends on the answer. The instrument that would settle it works and has not been run.

## Triage — current evidence and proposal — 2026-09-15

### User value and labels

A person running a persisted Pi session sees an extension prompt enter the waiting queue and leave it when the prompt closes. P3, sharpen: extend the existing gate report to the measured Pi extension-UI event pair, with its installation and coverage limits visible.

Retain `journey:stopped-at-gate`, `move:sharpen`, and `release:later`. Feature; current Low priority and L estimate retained pending the scope gate. No label, body, milestone, or blocking-relation writes were made in this triage.

### Current-state findings

- Read the live issue, empty comments page (hasNextPage=false), relations, owning milestone and project overview. No blockedBy or blocks edge exists. The overview has no literal “As of” block; its current What is next and Open decisions were read instead. DRC-4379's selection report records the complete 319-issue, 48-member, seven-open selection and the formal Pi/OpenCode tie; priority and the cached 0.2 are not tie-break evidence.
- Product tree is main `3c4abe2`. `collectors/pi.py` reads the complete session-header ID; `transcripts.pi_meta` supplies it. `events.IDENTITY_NORMALIZERS` admits Claude, Codex, Antigravity and Gemini only. Pi's registry flag remains false. No implementation branch exists for this triage.
- The issue's TypeScript and `EVENTS_BY_HARNESS` instructions are stale. Approved A-1 requires a first-class JavaScript adapter outside web/, and forbids inventing a Python hook-table entry for an in-process subscriber. DRC-4440 already taught the derived oracle both Python adapter shapes and admission intersection; JavaScript remains unseen. Preserve that improvement.
- A-3 remains real in current code: runtime discovery globs top-level *.py; lint_embedded only checks the web bundle; hook vocabulary validation consumes JSON; the harness-argument guard only sees event_hook.py commands. Widen all four existing surfaces with behavior exercised from the JavaScript adapter. Do not create another standing CI job.
- `test_contracts`' runtime import graph scans Python under cargento_runtime. A top-level JavaScript adapter with no new Python runtime module does not compel a new graph node or test file. It does not exempt the inventory and installed-copy checks.
- The old package `@mariozechner/pi-coding-agent@0.73.1` is deprecated and contains no ui_prompt events. The current official package is `@earendil-works/pi-coding-agent`; npm resolved 0.85.1, installed only under /tmp. Its shipped runner wraps extension select/confirm/input/editor/custom and coalesces nested prompts. The latter is source/documentation evidence, not a nested-prompt measurement.
- Official reference: [Pi extension events](https://pi.dev/docs/latest/extensions), plus the installed 0.85.1 `dist/core/extensions/runner.js`, `types.d.ts`, and `dist/core/project-trust.js`. Scope comes from the live drive below rather than from documentation alone.

### Live Pi measurements

Contained npm installs: /tmp/cargento-drc4380-pi (old-package negative) and /tmp/cargento-drc4380-pi-current (0.85.1). Controlled test root: /tmp/cargento-drc4380-probe, separate PI_CODING_AGENT_DIR, offline startup, telemetry disabled, explicitly loaded probe extension, built-in tools/context/skill/package discovery disabled. No shared harness config changed.

A stdlib Python PTY driver ran the actual official Pi CLI. The probe subscribed to actual events and registered one /capture command that sequentially awaited all five ctx.ui methods. It did not post to Cargento or imitate Pi's emitter. One model turn used a stdlib OpenAI-compatible SSE server at 127.0.0.1:48682 returning a fixed benign reply; no paid model request was necessary.

| Observed case | Start | End | Evidence boundary |
|---|---|---|---|
| select | 2 | 2 | Enter accepted in both sessions |
| confirm | 2 | 2 | Enter accepted first; Escape closed second |
| input | 2 | 2 | Submitted controlled text first; Escape closed second |
| editor | 2 | 2 | Escape closed both |
| custom | 2 | 2 | Custom component called done on keypress |
| stock project-trust screen | 0 | 0 | project_trust event fired; native trust screen visibly stood before session_start |

Observed event payloads have `type`, `reason:"ui_prompt"`, `kind`; select/confirm/input/editor carried the controlled title, custom omitted title. No answer text, native request ID, outcome, or timestamp was in the event itself. The recorder added receipt time and context identity separately. An end means the prompt closed, not that approval was granted. Do not publish prompt titles, answers, cwd or native session-file paths in the adapter envelope merely because the probe could read them.

The first /capture command ran before any model turn. Pi returned a session ID and prospective file path but had not persisted that file, even after appendEntry. Therefore a start event alone cannot create a collector row. After one ordinary local turn in the second run, `getSessionId()`, the actual JSONL header `id`, and Cargento /api/data's Pi `sid` were equal byte for byte: one whole 36-character UUID, not a basename prefix. Two independent session IDs were observed; only the second had a persisted identity join.

Stock trust was separately forced by a new disposable .pi/settings.json. The probe's project_trust handler returned no decision. While “Trust project folder?” stood open, no ui_prompt pair arrived and no session_start had occurred; choosing “Trust (this session only)” resumed startup. This contradicts the issue's claim that stock project_trust is covered by this pair. Do not intercept trust or create an alternative prompt to make that claim true.

Raw local evidence remains /tmp/cargento-drc4380-probe/events.jsonl (20 prompt events, plus session_start and recorder completion records), trust.jsonl, identity.json, terminal.raw, probe.js and drive.py. Events SHA-256: `29f2bdee66a6f724c01c5ce344c03236f475f52f5be5cb282e79a5727e6562d5`; identity SHA-256: `fad91fbe5e22baf1e14e86003c832799b9065c7fe2175185e55176f18ec04b21`. These scratch artifacts are not a committed capture corpus or a fresh-install oracle. Implementation must preserve the sanitized measured evidence with its procedure under docs/captures/pi before claiming shipment.

### Mode 1: before-development walk

Applied visual-review-and-fix to P3 and its Sessions/Attention surfaces, with the Chrome skill read before connection. Promise: “one queue of everything blocked on you, with what it is waiting for and how long it has waited. A session can also ask you a question directly and wait for your answer.” Shipped coverage is four harnesses; Pi is explicitly outside it.

Started the current tree on isolated port 48681 with isolated CARGENTO_HOME and the controlled Pi store. Quota fetching, model readings, git probes, terminal focus, history, dismissal, annotations, asks, tripwires and Spacedock were disabled. Read /api/data first: Pi discovered=true, reports_needs_input=false, one persisted Pi row with the exact measured ID. Other real local sessions remained present so this was not an empty-board test.

During the held select prompt, Attention said “0 waiting on you” and “10 of 12 sessions report block state · 2 unknown”. Expanding per-harness detail showed Pi: “Harness does not report blocks · token-rate reporting”. Sessions displayed “Controlled local identity capture.”, “generating…”, “Harness does not report blocks”, and “No block-state reading available”. Thus absence is disclosed, but the actual extension wait does not enter the queue: the intended build gap.

The walk covered a discovered-but-unreporting Pi, a standing extension prompt, and no reported gate. It did not manufacture a whole empty fleet, stale adapter receipt, missing adapter after capability flip, events-disabled final build, or an implemented appear/answer/clear path. Those latter cases remain explicit implementation acceptance work. No unrelated Mode 1 defect was promoted or filed. The source-only “generating” state follows the probe's appendEntry and is not a newly claimed collector bug.

### Draft rewritten issue body — not published

Publication assembly instruction (not part of the Linear body): take the sections from `#### User value` through `#### Historical context — 2026-09-15` below, promoting those headings to `##`. Under Acceptance, replace its assembly placeholder with all eight complete AC-1 through AC-8 bullets from this entity's canonical `## Acceptance criteria` section after the milestone draft, including each classification and Verified by clause. Under Historical context, retain the explanation of superseded claims and append the complete verbatim text under `### Original issue body — DRC-4380 — 2026-09-15` above. Omit these assembly instructions and placeholders from the published body. Do not publish the milestone draft, estimates, gate decision or stage report as part of the issue. This produces one self-contained issue body with its full criteria and dated history.

#### User value

A person running a persisted Pi session sees an extension prompt enter the waiting queue and leave it when the prompt closes. P3, sharpen: Pi joins gate reporting through its measured extension-UI events, with the coverage limits stated.

#### Build

Bundle a first-class JavaScript extension beside the dashboard's top-level scripts, installed through Pi's extensions mechanism. On the measured `ui_prompt_start`/`ui_prompt_end` pair, send the existing input_requested/input_resolved envelope directly to Cargento using the complete context session ID. The adapter observes prompts and never answers, blocks, replaces or opens one.

Admit Pi's measured whole ID in events.py, derive gate coverage from the actual JavaScript route intersected with server admission, then update the registry and prose count. Widen A-3's four existing validator surfaces. No fictitious EVENTS_BY_HARNESS row and no Python subprocess shim.

Serialize and bound transport so adjacent end/start notifications cannot overtake one another. Use the existing loopback/capability contract and allowlisted small scalar envelope, omit prompt contents, and isolate missing server/state, malformed capability, timeout and restart failures from the host UI. No active-wait rehydration across a dashboard restart is promised by a one-shot pair.

#### Coverage

Validated native version: `@earendil-works/pi-coding-agent@0.85.1` on macOS. This is an exact tested version, not evidence for every version at or above it. The deprecated 0.73.1 package does not provide the event surface; other versions require their own compatibility evidence.

This build covers blocking extension select, confirm, input, editor and custom prompts after Pi has persisted the session and Cargento can collect it. It excludes stock project-trust startup, prompts before persistence, arbitrary terminal input outside ctx.ui, and invented synthetic session rows. Show a Pi coverage qualification in the existing disclosure surface and document installation/version/store requirements. A quiet unsupported session must not be described as clear.

#### Acceptance

[Assembly placeholder: insert the complete AC-1 through AC-8 bullets from the canonical `## Acceptance criteria` section below; publish the bullets, not this placeholder.]

Preserve the measured event/identity capture and run the canonical pre-PR suite, sync-docs and Mode 2 before delivery.

#### Historical context — 2026-09-15

When publishing the approved rewrite, append the complete Original issue body preserved above under this dated heading. Its old TypeScript, Python hook-table and stock-trust claims are historical, not build directions. DRC-4190 remains the refuted predecessor; its cancellation is not reopened.

### Draft milestone correction — not published

Retain every other word in the original milestone description. Replace only DRC-4380's What is left line with:

[DRC-4380](https://linear.app/recce/issue/DRC-4380): See and clear extension UI prompts in persisted Pi sessions. The pair was measured on 0.85.1; stock project-trust startup and prompts before session persistence are outside this build.

No shared milestone overwrite is authorized here; implementation must re-fetch before applying the one-line correction so the OpenCode sibling's approved reconciliation is preserved.

## Acceptance criteria

- **AC-1 — interactive, user-visible:** A persisted Pi 0.85.1 session with the shipped adapter and a live dashboard enters Attention's waiting-on-you queue as “Needs input” during each supported prompt; its Sessions row names the wait and elapsed duration. Closing by answer or cancellation clears that wait, without claiming approval succeeded. **Verified by:** actual CLI drives select/confirm/input/editor/custom with before/during/after /api/data plus browser observations; omission of either mapped event fails the run.
- **AC-2 — offline:** Only measured kinds and the exact whole session ID reach Pi's admitted existing envelope; prompt titles, answers, paths and extra payload fields do not. **Verified by:** execute the actual adapter callbacks against a protocol fake and the real loopback admission/reducer, matching the captured native/header/collector join; mutate harness, malformed ID, unknown kind and content-bearing fields to prove refusal/omission.
- **AC-3 — offline:** A Pi capability declaration is demanded only when the shipped JavaScript gate route exists and the server admits it. **Verified by:** extend the existing derived registry oracle and mutation checks; remove the mapping, subscriber or admission, or change the adapter harness, and the declaration no longer validates. A hand-authored second literal set alone is insufficient.
- **AC-4 — offline:** Missing installed adapter, invalid JavaScript, wrong event vocabulary and wrong harness binding each fail the appropriate existing validation. **Verified by:** installed-copy inventory/discovery, node syntax check, source vocabulary/subscription check and harness-binding negative mutations in existing validator/linter suites, including comment-only false positives.
- **AC-5 — offline and interactive:** A server outage, malformed/missing state, stale capability, timeout or dashboard restart does not block Pi prompts or send to a non-loopback target. Prompt events remain bounded and ordered; consecutive end/start cannot clear a later wait. **Verified by:** controlled missing/hanging/403 loopback endpoints and race tests executing the real adapter; live UI remains responsive. Restart loss is disclosed, not portrayed as resumed coverage.
- **AC-6 — interactive, user-visible:** Coverage text identifies “extension UI prompts in persisted sessions” and requires the installed adapter on the validated version. Stock trust and a pre-persistence prompt do not produce invented rows; an unavailable reporter never reads as a measured absence. **Verified by:** browser walk with adapter installed/missing, events disabled, stock trust held and /capture before first turn, compared against the raw event and persistence observations.
- **AC-7 — offline:** Capture provenance retains observed event spelling and context/header/collector identity correspondence without user content or native local paths. **Verified by:** inspect sanitized captured records against this measured run, run the capture/privacy and documentation validators, and follow the committed capture procedure on a fresh contained install. Do not count a schema default or a fabricated fixture as an observation.
- **AC-8 — offline:** Existing harnesses retain their admission, precedence and capability behavior. **Verified by:** canonical AGENTS.md pre-PR suite once, with any named contention failure rerun alone; no unrelated byte-pin or runtime field edits. sync-docs updates owner docs rather than duplicating their contract.

### Expected surface, cost and review

Baseline assumes OpenCode lands the shared A-3/JavaScript-oracle foundation first; rebase and inspect it rather than duplicate its design.

| Surface | Pi incremental estimate | Compelled or chosen |
|---|---|---|
| Runtime | 3 files, +150–240/-0–20 lines: new top-level JS adapter, events.py normalizer admission, aggregate.py flag/qualification and prose count | Required for actual transport and published claim |
| Shared validator/oracle implementation | 2 scripts plus registry derivation helper, +40–100 lines if OpenCode foundation exists | A-3 and admission-aware derivation required; +180–300 if Pi must build foundation first |
| Behavioral tests | 1 new adapter test file and 4–6 existing files, +250–400 lines | New file is a chosen organization, not a mandatory gate; existing registry, events, validator, linter and installed-copy oracles compel updates |
| Documentation/capture | 5–7 owner files/capture records, +100–180 lines plus measured records | HOW_TO_USE, shipped skill, promise/compatibility/packaging owners as appropriate; capture privacy and links are existing gates |
| Frontend | 0 assets planned | Reuse reports_needs_input_when; if that cannot make AC-6 truthful, return to gate before touching byte-pinned web assets |

Tolerance: ±30% per category and at most two additional existing test/doc files; a new runtime module, any web edit, changed envelope schema, synthetic rows, trust interception, active-wait recovery, or >30% growth returns to the gate. Runtime and oracle budgets are separate; no total-line tolerance may hide compelled test growth.

Calibrated implementation estimate after shared foundation: 2–3 hours including capture replay, focused transport/security review and one full check cycle; 3–5 hours if Pi owns foundation. Uniform full adversarial treatment: 5–8 hours. These are estimates, not time already spent; remote CI/merge serialization and a failed live capture can add time. Capability/transport handling requires security-focused adversarial scrutiny; remaining conflict surfaces get two lenses and a reproducing arbiter per AGENTS.md. This triage used one worker and no delegates.

### Scope decision for the gate

Recommend approving the measured extension-only, persisted-session scope and the exact-version qualification. A-1/A-3 are settled and are not reopened. Stock trust exclusion is now an observed upstream boundary, not a preference to filter a signal that arrives.

The pre-persistence boundary is a material narrowing of the original user-visible criterion and must be explicitly accepted; triage has not accepted it on the captain's behalf. If the captain wants startup trust or pre-persistence waits, that requires a separately filed product decision and blocking relation before this build proceeds. No such Linear write was authorized in this stage; FO must route it instead of treating the broader power as implicitly granted.

Simplest rejected alternative: continue documenting Pi as unknown and ship no adapter. It preserves current honesty but does not put measured extension waits in the queue. Parsing idle transcript records cannot establish a standing prompt; a Python-spawning shim also violates the approved packaging choice.

## Stage Report: triage

- DONE: Establish the current Pi gate-adapter gap from live issue, milestone, approved packaging contract and before-development walk; probe real prompt events and session identity through a contained installation.
  Live 0.85.1 PTY produced 20 prompt events across five kinds/two sessions; persisted native/header/API IDs matched; browser showed Pi's explicit unknown block state.
- DONE: Preserve original Linear text verbatim and draft the narrow user-value rewrite, milestone correction, labels and offline/interactive acceptance criteria with concrete verification.
  Originals precede the analysis; AC-1 through AC-8 separate native/browser evidence from executable offline oracles; no Linear description/label mutation occurred.
- DONE: Commit a complete triage report with runtime/oracle surface estimates, tolerance, review depth, capture plan and unresolved decisions; leave product code and Linear rewrites for approval.
  Only this split-root entity is the deliverable; estimates cost compelled validator/test changes separately, and the gate must accept the narrower coverage.
- SKIPPED: Implementation, production capability flip, Linear rewrite, PR and approval.
  This dispatch authorizes triage only; no build or approval is claimed.
- DONE: Cleanup of owned processes and browser tab.
  Pi exited, model server 48682 and isolated dashboard 48681 were stopped; final lsof found neither listener; browser review tab was closed.

### Summary

The event pair is real for all five extension UI methods on the exact measured Pi 0.85.1 build, and one persisted session joined Cargento without truncation. Stock project trust does not emit the pair and extension prompts can precede persistence, so the proposed build is narrower than the original issue. Approve that boundary explicitly or route a separate product decision before implementation; the product tree remains unchanged.
