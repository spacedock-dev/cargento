---
title: "B2e(build) · OpenCode: report Needs input from the plugin permission pair"
linear-status: In Progress
milestone: Don't be the bottleneck
release: "later"
move: "sharpen"
promise: "P3"
estimate: "L"
source: "https://linear.app/recce/issue/DRC-4379/b2ebuild-opencode-report-needs-input-from-the-plugin-permission-pair"
status: triage
score: 0.2
started: 2026-09-15T03:41:50Z
completed: ""
verdict: ""
worktree: ""
issue: ""
pr: ""
mod-block: ""
reconciled: ""
gates:
    version: 1
    records:
        - id: gate:drc-4379:triage
          stage: triage
          attempts:
            - id: gate-attempt:drc-4379-triage-1
              briefing:
                id: briefing:drc-4379:triage:attempt-1:revision-1
                digest: sha256:8829d363baf4d6c32b9c662778d2309fd0a7aebeb9653b28cee22324d72856c8
                room-ref: ./drc-4379/review/triage/briefing-1
              resolution:
                type: Resolution
                id: resolution:spacedock:drc-4379:triage:1
                briefing: briefing:drc-4379:triage:attempt-1:revision-1
                by: person:captain
                at: "2026-09-15T04:09:30.328124Z"
                decision: approve
                reason: 'Captain: I approve these boundaries and drafts. Accepts the presented parent-session, per-project OpenCode scope, seven acceptance criteria, separate surface budgets and current official-binary evidence.'
              application:
                target-stage: implementation
                state: pending
---

[DRC-4379](https://linear.app/recce/issue/DRC-4379/b2ebuild-opencode-report-needs-input-from-the-plugin-permission-pair) — B2e(build) · OpenCode: report Needs input from the plugin permission pair

Seeded from the full Linear project read on 2026-09-15. Linear owns the current issue body and blocking relations; selection must fetch them before choosing work. No triage, approval, implementation, or delivery is claimed.

## Stage Report: selection

- DONE: Establish the current complete milestone membership and any interrupted reconciliation from fresh paginated Linear reads, distinguishing closed records and actual open blockers.
  Fresh 2026-09-15 Linear read: project 48468ef3-9b00-4a85-8d3f-3d90f8df8c8d, createdAt order, pages 100/100/100/19, final hasNextPage=false; 319 unique issues, 48 in milestone ca32ec9c-85c5-4c13-84ea-9ccc48c84f6f, comprising 38 completed, 3 canceled, 7 open.
- DONE: Recommend the first issue and a bounded delivery order for the remaining milestone, applying burndown ranking and identifying shared conflict surfaces, stale scope, and live-capture risks.
  Pick DRC-4379: release:later precedes five unlabeled issues; DRC-4380 ties on move:sharpen, no open dependents, no issue-specific panel score, L and Todo; operational tie-break is OpenCode's measured permission pair and reusable first-adapter checks, not Linear priority or a fabricated score.
- DONE: Commit a three-item selection report with the evidence, scope estimate, and concrete next triage target; change no product code and do not advance a gate or start implementation.
  This report alone is the deliverable, committed path-scoped to the split-root state branch; FO owns subsequent state transitions. Product tree inspected at main 3c4abe2; no implementation, live capture, issue write, gate advance, or new check was performed.

### Complete membership and reconciliation

Open: DRC-4379 and DRC-4380 are Todo, release:later, move:sharpen, L; DRC-4199 is Backlog, move:keep, XS; DRC-4203 and DRC-4208 are Backlog, move:keep, M; DRC-4200 and DRC-4194 are Backlog, move:none, XS. The latter five lack release labels despite being named in the milestone: a triage gap, not grounds to drop them.
Completed: DRC-4389, 4388, 4387, 4386, 4385, 4384, 4383, 4382, 4381, 4378, 4202, 4201, 4192, 4191, 4189, 4188, 4187, 4186, 4185, 4184, 4183, 4178, 4177, 4176, 4175, 4174, 4173, 4172, 4163, 4139, 4135, 4134, 4121, 4120, 4018, 4017, 4015, 4014. Canceled: DRC-4190, DRC-4019, DRC-4016. Closed work is not a dispatch candidate.
All seven full issue records and relations were read. Nobody is dropped for an open blocker: DRC-4379's two blockedBy edges target completed DRC-4427 (#290) and DRC-4428 (#292); the other six have none, and all seven have no blocks edges. RelatedTo is not dependency promotion.
The 66 current local entity records agree with fresh Linear status/milestone, all 49 project-open issues have entities, and none has pr set with reconciled empty. No state repair is handed to FO. Latest closed C6/C1/C4 receipts were read fresh: DRC-4025 comment 98a7a8f9, DRC-4020 bedaec88, DRC-4023 f2e4aa2a plus documentation follow-through 935deb02. Their completed debrief and archived records remain untouched; no new interruption was found.

### Delivery order and scope

Recommended order: DRC-4379 → DRC-4380 → DRC-4199 triage → DRC-4203 → DRC-4208 → DRC-4200 → DRC-4194. Release and move decide the groups; XS precedes M within unlabeled move:keep. The M pair formally ties, with shared reducer work placing 4203 first operationally; the final XS pair ties, with capture privacy first operationally. Re-rank on fresh relations after each delivery.
DRC-4379 next triage must preserve the approved A-1 decision in docs/design-adapter-packaging.md: first-class JavaScript outside web/, no subprocess shim, no fictitious EVENTS_BY_HARNESS entry. DRC-4428 is Done despite its stale body saying Todo; DRC-4440 (#298) already fixed Python adapter oracle admission. Its JS derivation gap and A-3's four validator surfaces remain build scope. events.py:356 still admits neither opencode nor pi nor droid.
OpenCode's docs/captures/opencode/plugin-permission-1.18.20-macos.jsonl is the existing measured basis. Triage still owes current installed-version compatibility, whole-id charset/collector identity, passive event subscription, per-project coverage disclosure, bounded transport behavior, and a live appear/answer/clear run. Child permission identity remains unmeasured: handle it with evidence or retain an explicit exclusion. No capture was run in selection.
Pi remains second, not dropped for the absent executable. It requires a contained install, a controlled loopback model server if needed, measured ui_prompt_start/end and session identity, plus a ruling on project-trust versus extension prompts. Existing pi/boot-envelope-fosession-linux.jsonl proves record shape only. The issue's instruction to add Pi to EVENTS_BY_HARNESS is stale under A-1. Documentation is not a gate capture.
Treat 4379/4380 as one adapter conflict surface for delivery planning: shared events.py admission, registry/oracles, validator inventory, syntax/vocabulary checks and install docs. A combined PR is worth considering only after separate triage/acceptance for both; otherwise serialize the shared foundation and reuse it. Do not merge their capture criteria into one result. 4203 and 4208 also touch events.py and should serialize against that foundation. Reserve at most one active web/ PR if 4199 still earns code work.
DRC-4199's named waitingQueue no longer exists. aggregate.py:1090 still publishes rounded age_sec without asked_at, but next-attention.js:76 ranks asks before input before considering ages. Direct execution of that actual comparator in Node at generated times 100.1, 100.6, 101.1, gate start 90 and question start 90.2 returned question,gate all three times (exit 0); removing the kind precedence would invalidate that result. This exercises the old cross-kind failure, not the full browser or a claim that absolute chronology is now provided. Triage should reconcile its user value and unchanged acceptance criteria with the replacement policy, not self-close it or implement the obsolete function.
DRC-4203 remains a real reducer gap by source: events.py ENDS_A_WAIT includes Working/Idle and needs-input has no deadline; statusline_hook.py:107-121 deliberately leaves confirmation unmapped. Clarify whether its accepted build includes the Antigravity mapping or only the prerequisite; its current ACs promise reducer precedence/expiry, not an additional adapter build.
DRC-4208 remains unrouted by source: droid absent from IDENTITY_NORMALIZERS and validate_hook_vocabulary's docstring still omits the shared-file limitation. Its identity fallback, harness containment and chosen packaging need their own tests/capture; do not assume a whole UUID solely from the first transcript record.
DRC-4200 needs a privacy-focused corpus audit and CI assertion, including its path-negative test and documented exceptions; no file was scrubbed here. DRC-4194 still has the unsupported “Pi, per the briefing” in audit/g2-detector.jsonl:11; correct attribution/demotion evidence only, preserving the dated items.json and items.bak.json panel records. Their parent B2 riskAdjustedImpact=60 is not an issue-specific score for either adapter; cached entity score 0.2 does not break the tie.

### Summary

The bounded next target is DRC-4379 triage, with seven open milestone members and no live relation blocker. Budget 6–10 hours remains a preliminary calibrated range (OpenCode 2–3h, Pi 2–3h, remaining triage/fixes/reconciliation 2–4h); uniform full review remains 12–18h, and live Pi/OpenCode capture failure or broader Droid containment can exceed either estimate. Refine after adapter triage and flag capture barriers early rather than treating absent evidence as completion.

## Linear edits made

No Linear description edit has been made. Verbatim pre-edit records fetched 2026-09-15; issue status is In Progress (FO lifecycle update).

### Original issue body — DRC-4379 — 2026-09-15

## User value

A person running an OpenCode session notices this when a standing permission dialog puts that session in the gate queue instead of leaving the board unable to say either way. P3, sharpen: gate detection reaches a sixth harness on evidence already captured.

## Why this exists

**The probe passed and no build was ever filed.** Copilot and Cursor each got a probe issue and a separate `(build)` issue; OpenCode got only the probe, [DRC-4188](<https://linear.app/recce/issue/DRC-4188/b2e-opencode-permissionasked-permissionreplied-the-only-explicit>), which closed Done on a captures-only PR (#167). Its own pass condition was met verbatim and nothing followed. `aggregate.py` still carries `reports_needs_input` unset for the `opencode` row.

The capture is `docs/captures/opencode/plugin-permission-1.18.20-macos.jsonl`: **positive on the whole question**. `permission.asked` carries `sessionID`, `permission.replied` carries `sessionID`, `requestID` and `reply`, the `requestID` is the ask's `id`, and in all three gated arms the `sessionID` is exactly a `session.id` row in `opencode.db`, taken by re-hashing the store's own rows rather than by eye. That is `input_requested` / `input_resolved` with no dwell, no expiry and no inference, and OpenCode is still the only harness on the board with an explicit resolve event.

Every store-only route is closed by the same capture, each measured while the dialog stood: the `permission` table held zero rows through all four arms including `Allow always`, the durable `event` table carries no permission kind, and no `session.status` event is delivered between the ask and the reply. So the plugin is the route, not a fallback.

## What needs to be done

**This list was corrected on 2026-09-07 after a scoping pass; four of the six original items were wrong. See the comment below for what each said and why.** The transport question is now a filed blocker.

Route 1, a bundled adapter, which OpenCode has not had before:

* A plugin file bundled under `cargento/skills/cargento/` for the user to drop in `.opencode/plugin/`. This is the **first non-Python shipped adapter**: `event_hook.py` serves hook-shaped harnesses invoked as a command, and OpenCode's plugin API is a subscription callback in the host process. The capture establishes that one plain file suffices and that no npm package is needed. Subscribe to the passive `event` hook, never `permission.ask`, which can change the gate decision: Cargento reports gates and never answers them.
* **Settle the transport first.** The gate oracle recognises only two routes and a plugin that POSTs directly is neither, so `reports_needs_input` cannot simply be flipped. That is a decision issue and it blocks this one.
* An `IDENTITY_NORMALIZERS["opencode"]` entry keyed on the **whole** session id. The capture records three distinct eight-character prefixes across nine store sessions, so a Claude-shaped truncation collides. A **new** normalizer, not the existing whole-id one: `_whole_uuid_sid` rejects anything not exactly 36 characters and the capture records a 30-character id. Its charset is not evidenced anywhere, so that has to be measured before the validation rule can be written honestly.
* `EVENTS_BY_HARNESS` is in `event_hook.py`, not `events.py`, and may be the wrong instrument entirely: that file is scoped to hook-shaped harnesses and its table is read only by `event_hook.py`. The transport decision settles this.
* Flip the registry row. **Nine oracles**, not four, now guard it: six in `HarnessGateCoverageTest` and three in `WaitDerivationReachTest`, two of them added after PR #266 for `reports_needs_input_when`. The capture's mechanism is per-project, so a `reports_needs_input_when` value is required and the fifth oracle's literal is in the blast radius.
* Two forced edits the original list missed: `scripts/tests/test_validate_plugins.py`'s discovery-parity assertion, whose walk globs `*.py` and will not find a bundled `.js`; and syntax checking, since `lint_embedded.py` only `node --check`s the web directory.
* Install documentation in `HOW_TO_USE.md`, which contains no OpenCode text at all today, and a vocabulary rule that `HOOK_FILE_VOCABULARY` cannot provide because it reads JSON through `load_json`.
* Doc blast radius: `SKILL.md:9`'s "four harnesses that can report that" becomes five, and `aggregate.py:208`'s "Six of the ten" becomes five, which oracle 3 pins.

**One free win the original list omitted.** `input_resolved` already exists in `events.py`, already maps to `OVERLAY_WORKING`, and `OVERLAY_WORKING` is already in `ENDS_A_WAIT`. No adapter maps it today. Since the capture confirms the asked and replied pair joins on the request id, `permission.replied` clears the wait with zero server-side change, and OpenCode would be the first user of that lane.

## Acceptance criteria

* **User-visible:** an OpenCode session held at a permission dialog appears in the gate queue as Needs input, and clears when the dialog is answered. **Verified by:** drive one OpenCode session to a permission prompt with the plugin installed, watch the row appear, answer it, watch it clear.
* The flip is proved by derivation rather than declared. **Verified by:** <issue id="e8ccc627-2344-44b4-b51f-1aa30ab27b7c" href="https://linear.app/recce/issue/DRC-4378/restore-the-reports-needs-input-oracles-and-the-comment-that-claims">DRC-4378</issue>'s oracle, which fails if the row is flipped without a route.
* An OpenCode subagent's permission event is handled or explicitly out of scope. The capture's record 15 says "whether a child's permission event carries the child id is untested". **Verified by:** the code says which, and the capture says why.

## Not in this issue

Antigravity's gate is also measured-positive and unbuilt, blocked on [DRC-4203](<https://linear.app/recce/issue/DRC-4203/a-wait-must-outrank-a-later-heartbeat-from-the-same-source-and-lapse>). Pi's is documented-positive and unbuilt. Each is its own issue.

### Original milestone description — 2026-09-15

## The user value

**One queue of everything blocked on you, with what it is waiting for and how long it has waited. A session can also ask you a question directly and wait for your answer.**

## What is left

[DRC-4379](<https://linear.app/recce/issue/DRC-4379>): See and clear OpenCode permission waits in the gate queue.

[DRC-4380](<https://linear.app/recce/issue/DRC-4380>): See and clear Pi prompts in the gate queue, after measuring the event pair.

[DRC-4208](<https://linear.app/recce/issue/DRC-4208>): Route Droid's hook events to the correct harness rather than silently losing them.

[DRC-4203](<https://linear.app/recce/issue/DRC-4203>): Keep a standing wait visible through ordinary heartbeats, with an explicit expiry.

[DRC-4200](<https://linear.app/recce/issue/DRC-4200>): Keep the captures supporting gate detection within their privacy contract.

[DRC-4199](<https://linear.app/recce/issue/DRC-4199>): Keep nearly simultaneous waits in a stable, truthful queue order.

[DRC-4194](<https://linear.app/recce/issue/DRC-4194>): Base the Pi gate-detection scoring audit on an actual source.

## Waits on

No open blocking relation is recorded on these issues.

## One question left open, gating nothing

Whether a raise survives the terminal that launched it. Cargento ships the socket case alone, so nothing that shipped depends on the answer. The instrument that would settle it works and has not been run.

## Triage proposal — 2026-09-15

### User value and labels

A person running OpenCode in a project with Cargento's adapter installed sees a standing permission request on the Attention page, then sees that wait clear after answering in OpenCode. P3, sharpen: the board can report another harness's waits while stating where the adapter must be installed.

Keep `journey:stopped-at-gate`, `move:sharpen`, `release:later`; feature, L. No label or description write is authorized by this report. In Progress was set by FO for ownership, not by this worker.

### Proposed issue body — not applied

## User value

A person running OpenCode in a project with Cargento's adapter installed sees a standing permission request on the Attention page, then sees that wait clear after answering in OpenCode. P3, sharpen: the board can report another harness's waits while stating where the adapter must be installed.

## Build

Ship one first-class JavaScript plugin beside the existing adapters, for installation in the project's `.opencode/plugin/` directory. Observe only the passive `event` callback: map `permission.asked` to `input_requested` and the matching `permission.replied` to `input_resolved`; never implement `permission.ask` or answer the permission from Cargento.

Use the whole OpenCode session ID, never its eight-character prefix. Add OpenCode's admission normalizer and conditional registry declaration. Reuse the existing overlay semantics; do not invent a gate reason from command arguments or claim that a reply proves tool success. Track matching request IDs within a bounded session-local set so duplicate requests or one reply cannot clear another standing request.

Follow the approved A-1 packaging decision. Widen the runtime inventory, JavaScript syntax check, adapter-vocabulary/route checks and gate derivation to see the real JavaScript adapter. Keep independent declaration and behavioral negative controls. Do not add an OpenCode row to `EVENTS_BY_HARNESS`: OpenCode does not invoke that command hook.

The adapter reads this run's OpenCode capability from the ordinary local state file, sends only the allowlisted envelope to IPv4 loopback, uses a bounded timeout/queue and ignores transport failure without delaying the permission decision. Missing state, disabled events and dashboard restart must fail quietly; no raw permission metadata, command, patterns, response text or capability enters a log.

Document the per-project install and uninstall, default port and optional local port override, and the condition in the coverage disclosure. No automatic configuration edit, global installation promise, child-session routing, Pi adapter, OpenCode question events, permission-answer button or terminal-control expansion is included.

## Acceptance

1. **Interactive, user-visible:** With the shipped adapter installed in a scratch project, a real standing OpenCode permission request makes the same session appear under Waiting on you as Needs input with a wait duration; answering in OpenCode clears that wait on the next delivered dashboard update. **Verified by:** record installed version, TUI request, payload identity and browser before/standing/answered observations; exercise allow and reject, with a no-permission control. A loopback model may produce the tool call, but OpenCode itself must produce the prompt and events.
2. **Offline:** Whole-ID admission joins the measured session to its collector row and refuses invalid/path-like IDs; another session sharing an eight-character prefix is unaffected. **Verified by:** parser plus real HTTP/application overlay tests using distinct full IDs; removing the normalizer or truncating it must fail.
3. **Offline:** Matching request resolution clears only the answered session's final outstanding request; duplicate asks/replies and unrelated events do not manufacture or clear a wait. **Verified by:** drive the actual plugin callback through overlapping request, duplicate and unknown-reply sequences and inspect delivered envelopes and resulting rows.
4. **Offline:** No credentials or permission contents escape the local event boundary, and missing/invalid state, disabled ingress, refused connection, slow server and restarted capability cannot hold up OpenCode or throw into its callback. **Verified by:** actual JS adapter with temporary state and recording loopback server, bounds assertions and negative controls; inspect destinations, body keys and captured diagnostics, including failure paths.
5. **Offline, user-visible:** Coverage says OpenCode reports only where its project adapter is installed and events are enabled; it does not claim installation was verified, child prompts are covered, or an unanswered session is safe. **Verified by:** assembled page tests asserting rendered coverage text for OpenCode with/without observations and with events disabled; remove the condition and observe failure.
6. **Offline:** Installed copies include the adapter, its syntax and vocabulary are validated, and the gate declaration is derived from an exercised JS route intersected with server admission. **Verified by:** run installed-runtime checks plus deliberate missing-file, syntax, wrong-route and no-gate-mapping mutations; the existing Python-only routes retain coverage.
7. **Interactive:** The documented per-project procedure is sufficient, uninstall removes future forwarding, and child permission routing is explicitly unclaimed. **Verified by:** install from an isolated plugin copy, start a fresh parent session, then remove the project file and start a control session; preserve the unmeasured child limit in the capture and reader-facing condition/docs. Children require a separate measured identity/routing change before any coverage claim.

## Historical context — retained 2026-09-15

The 1.18.20 probe in DRC-4188 / PR #167 measured the passive asked/replied pair and whole-ID store join. The original build body described transport as an open blocker; DRC-4428 / PR #292 settled first-class JavaScript, DRC-4427 / PR #290 fixed the collector tables, and DRC-4440 / PR #298 fixed Python adapter admission in the oracle. JavaScript derivation remains this build's work.

The original claims of a sixth reporting harness, four becoming five in specific old lines, and OpenCode being the only explicit resolve source were stale; counts must be recomputed from the current registry. The full original body is retained verbatim in the workflow's pre-edit record. The current 1.18.30 triage loaded the passive recorder and joined two session IDs to the collector, but both controlled prompt arms failed before tool execution in OpenCode's SystemPrompt.environment. Zero permission events in those failed arms is no evidence against the historical positive capture; current-version live acceptance remains unmet.

### Proposed milestone description — not applied

## The user value

**One queue of everything blocked on you, with what it is waiting for and how long it has waited. A session can also ask you a question directly and wait for your answer.**

## What is left

[DRC-4379](<https://linear.app/recce/issue/DRC-4379>): In projects with the adapter installed, see OpenCode permission waits in the gate queue and clear them by answering in OpenCode.

[DRC-4380](<https://linear.app/recce/issue/DRC-4380>): See and clear Pi prompts in the gate queue, after measuring the event pair.

[DRC-4208](<https://linear.app/recce/issue/DRC-4208>): Route Droid's hook events to the correct harness rather than silently losing them.

[DRC-4203](<https://linear.app/recce/issue/DRC-4203>): Keep a standing wait visible through ordinary heartbeats, with an explicit expiry.

[DRC-4200](<https://linear.app/recce/issue/DRC-4200>): Keep the captures supporting gate detection within their privacy contract.

[DRC-4199](<https://linear.app/recce/issue/DRC-4199>): Keep nearly simultaneous waits in a stable, truthful queue order.

[DRC-4194](<https://linear.app/recce/issue/DRC-4194>): Base the Pi gate-detection scoring audit on an actual source.

## Waits on

No open blocking relation is recorded on these issues.

## One question left open, gating nothing

Whether a raise survives the terminal that launched it. Cargento ships the socket case alone, so nothing that shipped depends on the answer. The instrument that would settle it works and has not been run.

### Mode 1 and current code evidence

Promise, verbatim from docs/promise-map.md P3: “one queue of everything blocked on you, with what it is waiting for and how long it has waited. A session can also ask you a question directly and wait for your answer.” Its shipped backing is the gate/ask queue, duration and limited session controls; its present gate reach is Claude, Codex, Copilot and Cursor. The headline stays unchanged; the OpenCode backing/limit changes only when delivery ships.

Before-development walk used main 3c4abe2 on spare port 48679, separate CARGENTO_HOME, with observer models, quota fetch, tripwires, git probe and focus disabled. Read /api/data before browser: 10 real rows, OpenCode reports_needs_input=false, no OpenCode store discovered. Chrome navigation by mouse reached Projects, Sessions and Attention, with the primary links visible on all three. Attention read “0 waiting on you”; expanding “per-harness detail” showed OpenCode “Harness does not report blocks · Token rate not reported”, while Codex stated “needs-input reporting, where approvals are enabled”. No quiet state was treated as universal clearance.

Restarted that same spare instance with the isolated OpenCode XDG_DATA_HOME and reloaded the browser. The collector published both real probe sessions. The active OpenCode row read “Harness does not report blocks” and “No block-state reading available”; the recent one read “QUIET”. This is an actual unavailable-reporting case with real rows, not synthetic payload injection. Screenshots are gitignored at docs/screenshots/drc-4379-triage-before-attention-coverage.png and docs/screenshots/drc-4379-triage-before-opencode-row.png.

Unwalked: a real standing OpenCode permission and its answer, events-disabled behavior on an installed OpenCode adapter, all-sessions-empty, and child prompts. No adapter exists yet for the latter installed cases; the current prompt rig failed before any tool execution. No unrelated pre-existing defect is folded into this build. Browser closed, both dashboard PIDs stopped, own tmux server and loopback model stopped; lsof showed neither 48679 nor 48680 listening.

Source facts: events.py:356 does not admit OpenCode; observation.py:358 derives capability publication from that admission table, so no separate hand-written token table is needed. input_resolved already produces Working and clears earlier waits permanently. No input_requested detail is carried, so generic Needs input is honest and raw command reason is out of scope. collectors/opencode.py:148-174 publishes top-level rows only and folds children into subagent summaries: blindly forwarding a child ID does not make a child row appear and must not be credited as child coverage.

The approved docs/design-adapter-packaging.md A-1 rules out the Python-spawning shim. A-3's four validator gaps remain in actual code; test_harness_registry.py:73-115 discovers Python adapters only. The current required registry module passes all 13 tests (python3 -m unittest cargento.skills.cargento.tests.test_harness_registry); this confirms the present non-reporting declaration, not the proposed JS route.

### Current contained capture and its limitation

Installed version: OpenCode 1.18.30, macOS, 2026-09-15. Temp root: /var/folders/pw/_rjswpfn53s0_xyrwtz8jvh00000gn/T/drc-4379-triage-5ho0qibj. Recorder lived only under its project/.opencode/plugin/probe.js; no shared configuration, product code or standing test changed. A clean child environment set XDG_CONFIG_HOME, XDG_DATA_HOME, XDG_CACHE_HOME, XDG_STATE_HOME and OPENCODE_CONFIG_DIR to that root, cleared inherited credentials/plugin roots via env -i, and selected a loopback-only provider. First launch still discovered shared skill directories read-only; the bounded retry set both observed OPENCODE_DISABLE_EXTERNAL_SKILLS and OPENCODE_DISABLE_CLAUDE_CODE_SKILLS flags in its own process.

Two plugin-load records, two session IDs, two SQLite session rows. Every event ID exactly equaled a store row and both were published by the shipped collector without truncation. Both IDs had length 30 and matched ses_ followed by 26 ASCII alphanumeric characters; that is a measured sample, not proof of every future vendor ID alphabet. A defensive full-ID grammar may admit this observed shape while rejecting path separators; no prefix lookup.

Both arms emitted busy then idle and failed before a permission prompt, with “TypeError: undefined is not an object (evaluating 'a.name')” in SystemPrompt.environment. The second launch added normal USER/SHELL and disabled external skill discovery; it failed identically. No raw model request was recorded and no user credential was used. The loopback server was never evidence of an OpenCode gate. Zero permission events across these arms is an instrument/setup limitation, not a refutation of DRC-4188's measured 1.18.20 pair. Whether the error is the custom-model metadata or another installed-harness defect remains unresolved.

Minimal recorder used (a temporary observation instrument, not an adapter implementation):

```javascript
import { appendFileSync } from "node:fs";
export const TriageProbe = async () => {
  const write = row => appendFileSync(process.env.CARGENTO_TRIAGE_EVENTS,
    JSON.stringify({...row, at: Date.now()}) + "\\n");
  write({kind: "loaded"});
  return {event: async ({event}) => {
    if (["permission.asked", "permission.replied", "session.created",
         "session.status", "session.idle"].includes(event.type)) {
      const p = event.properties;
      write({kind: event.type, keys: Object.keys(p),
        sessionID: p.sessionID || p.info?.id, id: p.id,
        requestID: p.requestID, reply: p.reply, status: p.status?.type});
    }
  }};
};
```

Private scratch files events.jsonl and data/opencode/opencode.db retain generated IDs for replay; only measured booleans, lengths, failure text and counts are retained here. Reproduce the identity proof by comparing the recorder's unique sessionID set against SELECT id FROM session in a fresh read-only SQLite connection and against /api/data OpenCode sid values with the same XDG_DATA_HOME. No transcript text or generated identity is committed.

The temporary opencode.json used only provider triage, npm @ai-sdk/openai-compatible, baseURL http://127.0.0.1:48680/v1, model probe with context 32000/output 2000, model triage/probe, permission bash=ask, enabled_providers=[triage], autoupdate=false. The stdlib HTTP server returned OpenAI chat-completion SSE chunks requesting one harmless bash tool (“printf cargento-probe”) and then a stop after a tool response. Launch was an isolated tmux TUI with --model triage/probe --prompt “Run the harmless bash probe command once.” Reconstruct those declared ingredients before rerunning; this temporary rig is not an undeclared required test dependency.

Current official provider setup reference checked: https://opencode.ai/docs/providers (local compatible provider/baseURL). New /v2/docs/providers uses a different schema, so do not mix that schema into the installed 1.x probe without verifying compatibility.

### Approach, estimates and review

Proposed runtime: one named-export JavaScript plugin file (approximately 180–260 added lines), events.py normalizer (20–40), aggregate.py registry/conditional coverage plus corrected prose (10–20). No new Python runtime module is proposed, so the test_contracts.py import-graph allowlist does not compel a new edge/module; no web asset edit or byte-pin recomputation is expected. OpenCode loads plugin function exports: keep helper constants internal rather than assuming arbitrary exported metadata is inert.

Cost the required infrastructure separately: scripts/validate_plugins.py and scripts/lint_embedded.py, about 100–180 added/changed lines across two files, to cover inventory, syntax, native names and fixed harness route outside web/. An executed callback/recording-server route is preferable to merely grepping JS comments for input_requested. The gate oracle must still reject a declared flag with no emitted route and a route the server refuses.

Tests: plan one new Python unittest module driving the real JS adapter under Node with temporary state and local servers, plus existing test_events.py, test_events_ingress.py, test_harness_registry.py, test_next_attention.py or test_next_sessions.py, scripts/tests/test_validate_plugins.py and scripts/tests/test_lint_embedded.py: approximately 7–8 files, 450–700 added/changed lines. Node already exists in the required syntax job; ordinary unittest runners without Node must report a skip honestly, while the required Node-enabled job must exercise the adapter. Existing runtime inventory parity compels its test edit; the Python import graph does not compel a new module. No blanket new CI job or standalone standing check is proposed.

Docs/capture: HOW_TO_USE.md, shipped SKILL.md, docs/promise-map.md backing, docs/design-adapter-packaging.md, docs/design-needs-input.md, SECURITY.md if its adapter boundary wording needs it, docs/captures/README.md and a new measured capture after successful live arms: approximately 6–8 files and 120–220 changed lines. Sync-docs determines exact owned claims; no shared docs-synced-through marker on the feature branch. The historical capture remains unchanged.

Tolerance: runtime ≤4 files / 350 changed lines; validator integration ≤3 files / 225; tests ≤9 files / 850; docs/capture ≤9 files / 280, with no new lifecycle vocabulary, no changed shared wait deadline, no child promotion, no generic adapter framework, and no frontend policy change. Crossing a category cap or changing those semantics returns to FO before implementation continues; do not conceal test growth within a runtime allowance.

Effort revised for DRC-4379 alone: 3–5h calibrated after a working live capture path, about 1h runtime, 1–1.5h oracle/validator integration, 0.5–1h live and browser acceptance, and 0.5–1.5h review/docs/CI. The failed setup probe is outside that forecast; it took two bounded attempts and has no reliable repair estimate. The milestone's earlier 6–10h estimate should be revisited with Pi triage rather than treated as a promise.

Review depth: new capability-token handling inside a third-party host merits the repository's security tier: security/transport and runtime/evidence lenses, a completeness pass, and an arbiter reproducing material findings. No reviewer agents were spawned in this triage. Pi shares validator and admission surfaces, but keeps its own capture and acceptance; serialize or combine only after both scopes are approved.

### Remaining decisions and next action

Recommend the narrow parent-session-only scope permitted by original AC 3, with that limit stated in coverage/install documentation. This does not reopen A-1 or DEC-2. Within-session overlapping requests must not be confused with child routing; bound and test their adapter bookkeeping.

The unresolved current-version capture barrier is the stop condition: establish a real 1.18.30 prompt using a corrected contained provider or a known supported model before crediting live ACs. FO should treat this as a disclosed failed triage demonstration, not waive it through an offline fixture. Captured session IDs alone do not prove current permission IDs. Do not expand this work into an upstream OpenCode repair without a separate decision.

Implementation should apply the approved issue/milestone drafts first, then work in its own branch, invoke the required TDD/design workflow and Mode 2 walk, and fulfill each numbered AC. No Linear draft or product change has been applied here.

## Stage Report: triage

- DONE: Establish the current OpenCode permission-adapter gap from live issue, milestone, approved packaging contract and before-development walk; demonstrate the riskiest passive-event and identity assumptions.
  Same-stage continuation below: official 1.18.30 allow/reject arms produced real standing prompts and matched asked/replied pairs; both IDs joined the shipped collector. The no-permission tool control emitted no permission events. Both failed Homebrew arms remain recorded.
- DONE: Preserve original Linear text verbatim and draft the narrow user-value rewrite, milestone correction, labels and offline/interactive acceptance criteria with concrete verification.
  Original issue and milestone are retained under Linear edits made; the proposed bodies and seven numbered ACs above separate real interactive acceptance from offline behavior and keep child routing explicitly unclaimed.
- DONE: Commit a complete triage report with runtime/oracle surface estimates, tolerance, review depth, capture plan and unresolved decisions; leave product code and Linear rewrites for approval.
  Path-scoped state report commit contains the proposal, source-backed category estimates, retained instrument failure and cleanup; main is unchanged and no description write, PR or gate approval occurred.

### Summary

The adapter gap remains real and A-1 is already settled. Initial Homebrew capture failures are preserved; the authorized same-stage continuation below established the current passive permission pair on the official same-version binary and removes that demonstration barrier. The narrow draft and its costs are ready to review, with no product or Linear rewrite applied.

### Same-stage continuation — 2026-09-15 04:00–04:06 UTC

FO authorized at most 20 minutes, zero additional agents, source-guided temporary setup changes only. The investigation and live controls completed in approximately six minutes. This continuation supersedes the earlier current-version capture stop condition and the draft's claim that the live pair remains unmeasured; it does not erase the failed arms or establish implementation acceptance.

**Cause narrowed before choosing another run.** The installed Homebrew formula is v1.18.30 revision 1, rebuilding source with Bun. Its bundled SystemPrompt.environment matches [the tagged source](https://github.com/anomalyco/opencode/blob/v1.18.30/packages/opencode/src/session/system.ts#L62): it initializes project-reference location services before using model data. The failing `a.name` is the dependency-node resolver in [LayerNode.hoist](https://github.com/anomalyco/opencode/blob/v1.18.30/packages/core/src/effect/layer-node.ts#L221), reached from [buildLocationServiceMap](https://github.com/anomalyco/opencode/blob/v1.18.30/packages/core/src/location-services.ts#L91). This is an undefined dependency-tree node, not evidence of incomplete custom-provider model metadata. The exact missing node/build cause was not repaired or determined.

**Controlled binary comparison.** Downloaded only into the existing temporary root: [official opencode-darwin-arm64.zip, v1.18.30](https://github.com/anomalyco/opencode/releases/download/v1.18.30/opencode-darwin-arm64.zip), 46,240,880 bytes. Archive SHA-256 `a5e43d6887386efc7d68ce49ae28e3bbdfdee3dfd1d7169b612c3ce67e53b1e8` exactly matched the GitHub release asset digest. Extracted executable reported 1.18.30. The installed /opt/homebrew binary was not replaced, and no source, shared configuration or security setting was patched. Same temporary provider, model, passive recorder and second-arm environment were used; only executable and output filename changed. The official build passed where the Homebrew build failed. This demonstrates a build-dependent failure in this setup; it does not identify the underlying build defect or establish that every Homebrew installation fails.

| Current official 1.18.30 arm | Real UI/control | Passive events and identity |
| -- | -- | -- |
| Allow once | TUI displayed “Permission required” and “Shell command”; after Enter it printed the harmless marker and “Probe complete.” | One permission.asked, no reply while standing, then one permission.replied with reply=once; requestID equals ask.id, same full sessionID; 20,966 ms ask-to-reply. |
| Reject | TUI displayed the same real prompt; selected its visible Reject choice with Right, Right, Enter; prompt cleared. | One asked and one replied with reply=reject; requestID equals ask.id and same full sessionID; 33,213 ms ask-to-reply. |
| No permission | Same real harness and recorder, only this scratch project's bash permission changed from ask to allow; the tool completed with exact output cargento-probe, and session.idle fired. | One plugin-load record, zero permission events. No prompt was visible at the final read; zero events spans the whole recorded arm, while UI absence was sampled rather than continuously filmed. |

Asked keys were id, sessionID, permission, patterns, metadata, always, tool. Replied keys were sessionID, requestID, reply. Both permission session IDs were present in the real read-only SQLite session table and the shipped collector payload. All five current probe session IDs (two failed Homebrew arms, three successful official controls) were distinct whole IDs but shared one eight-character prefix. Both gated IDs had the observed 30-character ses_ plus alphanumeric form. This settles the whole-ID requirement on current permission events, not merely session.created.

**Real-wait Mode 1 observation.** While the reject prompt stood, /api/data on spare 48679 published that same OpenCode session as working, blocked_since=null and reports_needs_input=false. The unmodified board row showed “generating…” and “Harness does not report blocks / No block-state reading available”. This is a current real gate the board cannot report, and the absence is explicitly disclosed. Screenshot: docs/screenshots/drc-4379-triage-current-permission-baseline.png (gitignored). No adapter-to-Cargento POST was implemented or simulated to make the board pass.

**Revised proposal application note.** In the proposed issue's Historical context, keep the two failed Homebrew attempts and add: “A same-stage comparison with the official 1.18.30 release succeeded: the passive asked/replied pair joined the full collector ID on allow and reject, and an allowed-tool control emitted neither event. The tested Homebrew 1.18.30_1 build failed in dependency initialization before tool execution; the official binary provides the contained live verification path.” Remove only the draft sentence saying current-version live acceptance is unmet because no prompt could be reached. Full **shipped-adapter** show/answer/clear acceptance remains implementation work; the recorder proved the harness path, not an adapter that does not exist.

**Scope retained.** No child prompt was driven, no Allow always arm was added, no question event was measured, and no forwarding or capability-token code was built. Existing seven ACs remain intact; official binary provenance must accompany the implementation's live proof rather than silently presenting it as the installed Homebrew executable. The 3–5h estimate now has a working contained capture path; no general setup-repair estimate is claimed.

**Cleanup and evidence.** Both dashboard incarnations from the first pass and this continuation's PID 9806 were stopped. The private tmux server drc4379triage and loopback model process were stopped; Chrome review tab closed. Private scratch keeps the official archive/executable, raw generated-ID event records and summarized proof JSON for independent replay; none of those identities, prompt transcripts or executable bytes entered this git commit. Only this entity body is changed. No paid inference, upstream repair, gate approval, Linear rewrite or product edit occurred.
