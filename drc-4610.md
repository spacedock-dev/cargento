---
id:
title: "An acceptance criterion names a Console path that has had no caller since 8d2585c"
status: implementation
source: "https://linear.app/recce/issue/DRC-4610/an-acceptance-criterion-names-a-console-path-that-has-had-no-caller"
started: 2026-09-17T14:06:44Z
completed: ""
verdict: ""
score: 0.6
worktree: ""
issue: ""
pr: ""
mod-block: ""
linear-status: "Backlog"
milestone: Clean and Cogent UI/UX
release: ""
promise: "P2"
move: "keep"
estimate: ""
reconciled: ""
gates:
    version: 1
    records:
        - id: gate:drc-4610:triage
          stage: triage
          attempts:
            - id: gate-attempt:drc-4610-triage-1
              briefing:
                id: briefing:drc-4610:triage:attempt-1:revision-1
                digest: sha256:aa77e19f08a8240ede82226eb8505b77b2dee5410912e683f07511cafd431e0b
                room-ref: ./drc-4610/review/triage/briefing-1
              withdrawal:
                by: agent:first-officer
                at: "2026-09-17T17:02:59.259409Z"
                reason: The stage report names AC-1 and AC-6 literally while describing its own checklist compliance, so --ac-scan reads both as evidenced by triage. Presenting a briefing that reports two of six criteria as already evidenced, at the gate where every one should read unevidenced, is the false-completeness defect this milestone exists to remove. Withdrawing to reword the report line, then re-preparing.
            - id: gate-attempt:drc-4610-triage-2
              briefing:
                id: briefing:drc-4610:triage:attempt-2:revision-1
                digest: sha256:8e9efc16d482dc459efe61efdc5ef744f0d19ad5385312c2df0ef2febc1bcb0d
                room-ref: ./drc-4610/review/triage/briefing-2
              resolution:
                type: Resolution
                id: resolution:spacedock:drc-4610:triage:2
                briefing: briefing:drc-4610:triage:attempt-2:revision-1
                by: agent:first-officer
                at: "2026-09-17T17:04:01.023556Z"
                decision: approve
                reason: 'Approved with all six asks ruled. (1) Delete, not wire back: 6c7bd9c replaced the card with a rail rendering the same absence from the shared derivation model, and two cards printing one figure from two derivations is the disagreement that refactor existed to end. (2) Yes, this issue may correct DRC-4589''s Linear body - the inverted tab labels are published there too and a correction landing only on DRC-4610 is worthless. (3) Its own PR, after the integration: DRC-4589''s work is already inside a six-pick consolidated integration in flight, and folding an unimplemented issue into it now would destabilise a resolution that is already carrying four branches. The byte-pin saving does not outweigh that. (4) Set blockedBy: DRC-4589 - relatedTo does not stop anyone picking it up and the dependency is real. (5) Title changes; the current one names a commit this triage falsified and there appear to be no external references. (6) Leave the milestone description to sync-project, which owns tracker accretion. Approved chiefly because the attribution was proven by walking the occurrence count commit-by-commit rather than sampling the endpoints, which is what caught that the title named the wrong commit.'
                conn:
                    quote: I pre-approve all the triage and merge gates, just automate this entire process and do it
                    source: 'Captain''s answer to the burndown dispatch question at the start of this session, reaffirmed later as: if you approve that the PRs are good and can be merged, than go ahead and merge the PRs. do not gate on me.'
              application:
                target-stage: implementation
                state: consumed
---

[DRC-4610](https://linear.app/recce/issue/DRC-4610/an-acceptance-criterion-names-a-console-path-that-has-had-no-caller) — An acceptance criterion names a Console path that has had no caller since 8d2585c

Filed 2026-09-17 by DRC-4589 implementation while verifying its own acceptance criterion. Two things may be wrong and the issue forces them apart: the code may be dead, or the criterion may be aimed at the wrong place and was verified against a function no reader meets. The second is invisible while the test is green, and deleting the code first would hide it permanently.

## Linear edits made

Nothing has been written to Linear. This section is the pre-edit record, captured 2026-09-17
before any drafting, followed by the drafts. `implementation` writes the drafts after the gate.

### Captured original — DRC-4610 title

````text
An acceptance criterion names a Console path that has had no caller since 8d2585c
````

### Captured original — DRC-4610 body (verbatim)

````markdown
## User value

No user sees this change and the move is `keep`. It matters because a criterion aimed at dead code certifies nothing: the test passes, the gate reads it as evidence, and the surface the criterion was written to protect is not on screen at all.

## The problem

`nextProjectDelegation` at `next-delegation.js:168-186` is the function <issue id="53b75c53-66ba-4dce-a929-2dc0e89a453e" href="https://linear.app/recce/issue/DRC-4589/split-the-three-inks-onto-label-value-and-absence-roles-so-a-label">DRC-4589</issue>'s AC-2 names as the Console "no figure yet" path. It **has no caller anywhere in the runtime**, and has not since `8d2585c`.

It is stamped and unit-tested, but only by direct call. So the test is green, the criterion reads as satisfied, and nothing establishes that a reader ever meets the markup it renders. <issue id="53b75c53-66ba-4dce-a929-2dc0e89a453e" href="https://linear.app/recce/issue/DRC-4589/split-the-three-inks-onto-label-value-and-absence-roles-so-a-label">DRC-4589</issue>'s implementation found this while verifying its own criterion rather than while reading the file, said so in the test's docstring so the test does not read as covering a rendered path, and filed it rather than deleting anything.

Two distinct things are wrong and they need separating before either is acted on:

* **The code may be dead.** If nothing calls it and nothing should, it goes, along with its test and its stamp.
* **The criterion may be aimed at the wrong place.** If the Console genuinely renders a "no figure yet" absence, it does so by some other path, and <issue id="53b75c53-66ba-4dce-a929-2dc0e89a453e" href="https://linear.app/recce/issue/DRC-4589/split-the-three-inks-onto-label-value-and-absence-roles-so-a-label">DRC-4589</issue>'s AC-2 was verified against a function that is not the one a reader sees. That is the more serious half, and it is invisible while the test is green.

This is the same family as the guards this milestone has already found: a check that passes while the claim it stands for is untested. Five of those have now surfaced here, and every one was green and mutation-checked.

## The solution

Establish which of the two it is, in this order, because the answer changes what ships.

1. Find what actually renders the Console's absent-figure state today. If it is another function, <issue id="53b75c53-66ba-4dce-a929-2dc0e89a453e" href="https://linear.app/recce/issue/DRC-4589/split-the-three-inks-onto-label-value-and-absence-roles-so-a-label">DRC-4589</issue>'s AC-2 was verified against the wrong target and the record needs correcting — say so plainly rather than quietly repointing it.
2. Then, and only then, decide the code. Remove it with its test and stamp if nothing renders it; wire it back if the Console lost a path it should have.

The rejected alternative is deleting it now on the strength of the caller count. That closes the cheap half and permanently hides the expensive one: a criterion verified against an unrendered path leaves no trace once the path is gone.

## Acceptance

* **AC-1 — offline:** The function that renders the Console's absent-figure state is named, and it is reachable from a caller in the runtime. **Verified by:** a call-graph trace from the Console render entry point to the emitting function, recorded in the issue. **Falsified by:** naming a function whose only caller is a test.
* **AC-2 — offline:** Either `nextProjectDelegation` and its test and stamp are gone, or it has a runtime caller. **Verified by:** `grep -rn "nextProjectDelegation" cargento/skills/cargento/cargento_runtime/` returning either nothing or a call site outside its own definition. **Falsified by:** the definition surviving with test-only references.
* **AC-3 — offline:** If AC-1 finds a different emitter, <issue id="53b75c53-66ba-4dce-a929-2dc0e89a453e" href="https://linear.app/recce/issue/DRC-4589/split-the-three-inks-onto-label-value-and-absence-roles-so-a-label">DRC-4589</issue>'s AC-2 record carries a dated correction naming what it was actually verified against. **Verified by:** the correction present on that issue. **Falsified by:** the criterion silently repointed with no record that it had been verified elsewhere.

## Provenance

Filed 2026-09-17 from <issue id="53b75c53-66ba-4dce-a929-2dc0e89a453e" href="https://linear.app/recce/issue/DRC-4589/split-the-three-inks-onto-label-value-and-absence-roles-so-a-label">DRC-4589</issue>'s implementation, which found it while verifying its own acceptance criterion rather than while reading the file, and reported it instead of quietly satisfying the criterion by direct call.
````

### Captured original — milestone `Clean and Cogent UI/UX` description (verbatim)

````markdown
## The user value

**The board can be read at a glance: you can tell a label from its answer, a figure from a gap, and the one thing to do on a screen from the prose around it.**

Users report low contrast. Every text ink in the cockpit passes WCAG AA — ink3, the dimmest, is 5.67:1 on panel, nearer AAA than the AA floor. Measured size-aware with APCA, the same inks fail badly at the sizes they are actually set in: at 12.5px/400 even `#f4f1e8`, the brightest ink in the palette, scores Lc 98.0 against a body-text requirement of 100, and the 9.5px and 10px label steps fall off the bottom of the lookup table entirely.

So there is no colour left to spend and no contrast fix available. The work is to raise the sentence tier, collapse the label tier, and give label, value and absence three distinct registers instead of the one dim ink that currently carries 89% of the characters on Held to.

## What is left

Fifteen issues. Three are foundations and everything else depends on them:

* **Raise board sentences to a 15px tier** and collapse six sub-12px tokens into one label tier.
* **Split the three inks onto label, value and absence roles**, so a label stops sharing its ink with its own answer.
* **Always render the Held to reading control**, present and inert with its reason, instead of deleting the tab's only verb exactly when a newcomer looks for it.

Then: the briefing, the tab strip, a control primitive with a primary action on the one tab that has one, caveat tiering, Held to ordering, Console ordering, the scope rail, the timeline filter, and a guardrail so the asset test enforces size rather than the contrast that already passes. Two issues were split out of the primitive at triage: giving the other four tabs a main action to mark, which is a product question rather than a styling one, and the ten control recipes the primitive's enumerated set does not reach.

## Waits on

Nothing outside this milestone. `type-scale` and `ink-roles` gate most of the rest; `ink-roles` itself waits on `type-scale`.

## How this was measured

Contrast computed from the shipped tokens and independently recomputed during triage, matching to the decimal. APCA from the same pairs at the sizes they render at — **audit-only: no APCA implementation, table or fixture exists in the repository, so these figures cannot be reproduced from the tree.** No issue in this milestone adds an APCA implementation, so these figures stay audit-only. [DRC-4596](<https://linear.app/recce/issue/DRC-4596/make-the-asset-test-enforce-font-size-not-the-contrast-that-already>) guards the **px floors** the audit motivated — the 11px label tier and the 15px sentence tier — which is a different and weaker property than reproducing the audit. It cannot see a sentence whose size, family and line-height are composed across several rules; two such sentences remain and [DRC-4602](<https://linear.app/recce/issue/DRC-4602/sans-rules-still-sit-below-the-15px-sentence-floor-and-a-per-rule>) sizes them. Density, ink distribution and control counts read off the live DOM at `127.0.0.1:4553`. Density figures are floors — they were taken on a quiet, unannotated session, and a busy project has more text, not less. Control counts are floors in the same way and are **data-dependent**: retaken on a three-session project during [DRC-4595](https://linear.app/recce/issue/DRC-4595/put-consoles-operations-first-and-lift-the-steer-box-out-of-tripwires)'s triage, Console's 5 reproduced but Now and Course came back 3 and 3 against the 0 and 1 on file. They illustrate the imbalance; no acceptance criterion in this milestone may rest on one.

Eleven claims were raised and rejected rather than filed. The two worth knowing: brightening the inks cannot fix C1, and merging the three thin tabs is a route change plus an amendment to NUI-3, not a layout change.
````

### Captured original — milestone comment `Read before building`

One comment exists on the milestone, posted 2026-09-17T11:18:20Z, id `38cca5e4-fb02-46e9-8fb0-3aa6c57c9c3e`. It is
not reproduced in full here because nothing in this rewrite edits it; its four bullets key on
DRC-4597, DRC-4602, DRC-4596 and "every issue after DRC-4587", and its own preamble records why it
is a comment: `save_milestone` has no patch operation and resends the whole body, so a comment is
additive and carries no re-serialization risk. `implementation` appends one bullet to it rather than
rewriting it — the draft is under **Drafted milestone comment bullet** below.

The fourth bullet — "measure against the tree the work will land on, not against `main`" — was
followed here in its strict sense and then overtaken by events: DRC-4587 landed as `84d27a5`
(2026-09-17), so `origin/main` **is** the post-4587 tree, and every line number and figure in this
entity was read from `84d27a5` rather than from a stacked branch. The one exception is named where
it appears: the DRC-4589 test evidence was read from the unmerged branch `spacedock-ensign/drc-4589`,
because that is the only tree the test exists on.

## Adversarial read

Everything below was read from `84d27a5`, the merged `main` tip and the first commit after DRC-4587
landed. Where a claim needed the unmerged DRC-4589 branch, that is said at the claim.

**The problem is still real, and it is smaller and more specific than the issue as filed says.**
Four of the issue's claims survive, three do not, and one whole half of its central worry is
already answered by tests that were on `main` before it was filed.

**F1 — the commit in the title is wrong.** The caller survived `8d2585c`. `git grep -c` over
`cargento_runtime/web/` returns **2** occurrences at `8d2585c` (`next-delegation.js:155`, the
definition, and `next-project.js:185`, a call inside the project aside) and **2** at its parent. It
returns **1** at `6c7bd9c` and **1** at `84d27a5`. The caller was dropped by `6c7bd9c` — *feat(next):
refactor the dashboard to the v2 project-level design* (#314, 2026-09-08) — which replaced the
project aside with the project rail. So the function has been unreachable for nine days, not
sixteen, and by a different commit than the title names.

**F2 — the Console's absent-delegation figure is emitted by `nextRailDelegation`, and every hop to
it is a runtime call site.** The trace, all line numbers on `84d27a5`:

````text
next-project.js:397          nextProjectCockpit(context, observation, commandAttention)
next-cockpit.js:3347         nextProjectCockpit  ->  nextCockpitPanel(context, focus, ...)
next-cockpit.js:3311-3342    nextCockpitPanel    ->  else-branch (tab === "console")
next-cockpit.js:3341             ... + nextProjectRail(context)
next-delegation.js:284-291   nextProjectRail     ->  nextRailDelegation(project)
next-delegation.js:202-223   nextRailDelegation  ->  !metric.pctKnown branch at :207-209
next-observed.js:216-236     metric              =   project.delegation, where
next-observed.js:221             pctText: known ? `${pct}%` : "no figure yet"
````

`nextProjectRail` is reached from exactly one place, the Console branch, so the project rail *is*
the Console tab. The issue as filed, and DRC-4589 before it, both label `:209` "(project rail)" and
`:182` "(Console tab)". That mapping is inverted: `:209` is what the Console renders and `:182` is
what nothing renders.

**F3 — two rendered-route guards already stand on `main`, and both agree with F2.**
`tests/test_next_cockpit.py::test_unmeasured_delegation_keeps_the_shared_models_reason` (:695) sets
`nextRoute = {view:"project",project:"cargento",tab:"console"}`, calls `renderNext()` over the real
route, and asserts the page carries "no figure yet", the shared model's own `noteText`, and
**exactly one** `data-next-delegation-withheld`. `test_v2_surfaces_mount_in_their_cockpit_panels_once`
(:833, :840) asserts `data-next-project-rail` appears on the Console panel and on no other tab, and
that the string `"DELEGATION · "` — the dead card's header format at `next-delegation.js:178`, which
the rail's header does not produce — is **absent** from that panel. So the naming half of this
issue was answerable from the suite without a new test.

**F4 — the charge against DRC-4589 is narrower than filed, and the filed wording would libel it.**
On `spacedock-ensign/drc-4589`, that issue's absence-stamp criterion is carried by
`BothWithheldFiguresCarryTheAbsenceStampTest` (`tests/test_next_delegation.py:798`), which has
**two** positive arms, not one: a direct call to `nextProjectDelegation` (:814), whose docstring
already records that the function has no caller and that the assertion is stated as a unit call; and
a `renderNext()` arm on the rail (:842). So it is false that the criterion "was verified against a
function that is not the one a reader sees" — half of it was verified against the emitter a reader
does see.

What *is* true, and is the finding worth keeping: **neither arm routes through the Console panel.**
`RAIL_FIXTURE` (:9-21) assigns `nextProjectView = () => nextProjectRail({group:{label:"alpha/repo"}, …})`,
so the `renderNext()` arm renders the rail through a replaced view function and never enters
`nextCockpitPanel`. One arm exercises a function no route reaches; the other exercises a reachable
function through a route the fixture removed. Both would still pass if `nextCockpitPanel`'s console
branch stopped emitting `nextProjectRail(context)` altogether.

**F5 — a second function dies with it, and the issue does not mention it.**
`nextDelegationRateMarkup` (`next-delegation.js:159-166`) has exactly one caller in the repository:
`nextProjectDelegation` at :193. `nextDelegationTrendMarkup` (:151) is **not** in that position — the
rail calls it at :214 — so it stays.

**F6 — nothing orphans in the stylesheet.** Each of `.next-delegation-withheld`,
`.next-delegation-figure`, `.next-delegation-metrics` and `.next-delegation-caption` has two uses in
`next-delegation.js`, one in each card. Deleting the dead card takes each to one. `styles.css` does
not change, and its pinned size and digest do not move.

**F7 — no required check could have caught this, so "add a guard" is a real choice rather than a
repair.** `scripts/lint_embedded.py` has no unused-symbol rule, and the `web/` parts are
concatenated through `APP_PARTS` rather than imported, so there is no import graph to walk. Nothing
compels a new test file here: the routed assertion belongs in the existing `test_next_cockpit.py`.

**F8 — this issue is blocked by DRC-4589, and the block is mechanical.** The test being deleted and
the `data-next-absent` stamp at `:182` exist only on `spacedock-ensign/drc-4589`. Deleting the
function here before that branch lands would either conflict on the same span of `next-delegation.js`
or silently drop work that has not yet been reviewed. `next-delegation.js` is under
`cargento_runtime/web/`, which AGENTS.md restricts to one in-flight PR at a time, so the two
serialize anyway. Set `blockedBy: DRC-4589` at implementation.

**Two claims in the filed body are not checkable and should not survive the rewrite.** "Five of
those have now surfaced here, and every one was green and mutation-checked" counts guards found
across the milestone; no list of the five is on file, and this triage did not reproduce the count.
And "It is stamped and unit-tested" is true only of the unmerged branch — on `main` the function has
no test at all.

## Drafted rewrite — DRC-4610 title

````text
A Console delegation card has had no caller since 6c7bd9c, and two tests stand on paths no route reaches
````

## Drafted rewrite — DRC-4610 body

````markdown
## User value

No user sees this change, and the move is `keep`. What it protects is the evidence behind the rest
of this milestone: a criterion checked against code no route renders certifies nothing, and reads
at the gate exactly like one that certifies something. This milestone exists to remove confident
wrong answers from the board; this is the same defect one layer up, in the board's own tests.

## The problem

`nextProjectDelegation` (`next-delegation.js:168-195`) renders a DELEGATION card with its own
"no figure yet" absence at `:182`. **Nothing calls it.**
`git grep -n nextProjectDelegation` over the repository returns its definition and nothing else — no
runtime call site, and on `main` no test either.

Its last caller was `next-project.js:185`, inside the old project aside. `6c7bd9c` —
*feat(next): refactor the dashboard to the v2 project-level design* (#314, 2026-09-08) — replaced
that aside with the project rail and dropped the call. **It was not `8d2585c`**, which this issue
named when filed: the call site is present at `8d2585c` and at its parent, and the occurrence count
over `cargento_runtime/web/` goes 2 → 2 → 1 across `8d2585c^`, `8d2585c`, `6c7bd9c`.

One more function dies with it. `nextDelegationRateMarkup` (`:159-166`) has exactly one caller,
`nextProjectDelegation` at `:193`. `nextDelegationTrendMarkup` (`:151`) is not in that position: the
rail calls it at `:214`.

Nothing in CI could have seen either. `scripts/lint_embedded.py` carries no unused-symbol rule, and
the `web/` parts are concatenated through `APP_PARTS` rather than imported, so there is no import
graph to walk.

## What the Console actually renders

The Console tab's absent-delegation figure comes from `nextRailDelegation`, and every hop to it is a
runtime call site:

`next-project.js:397` → `nextProjectCockpit` (`next-cockpit.js:3347`) → `nextCockpitPanel` (`:3311`)
→ the `console` branch at `:3341` → `nextProjectRail` (`next-delegation.js:284`) →
`nextRailDelegation` (`:202`) → the `!metric.pctKnown` branch at `:207-209`, which prints
`metric.pctText` — `"no figure yet"` from `next-observed.js:221`.

`nextProjectRail` is reached from that one place, so **the project rail is the Console tab.** Both
this issue as filed and DRC-4589 label `:209` "(project rail)" and `:182` "(Console tab)". That is
inverted.

Two tests already on `main` say the same thing from the rendered route.
`test_unmeasured_delegation_keeps_the_shared_models_reason`
(`tests/test_next_cockpit.py:695`) renders the Console route and asserts exactly one
`data-next-delegation-withheld`, carrying the shared model's own reason.
`test_v2_surfaces_mount_in_their_cockpit_panels_once` (`:833`, `:840`) asserts the rail mounts on
Console and nowhere else, and that `"DELEGATION · "` — the dead card's header format at `:178` — is
absent from that panel.

Two on-screen tells separate the cards, for anyone checking by eye rather than by grep: the dead
card's header is `DELEGATION · {WINDOW}` and its reason reads "Waiting **for** one complete
token-rate window"; the rail's header is a bare `DELEGATION` with the window in its meta slot, and
its reason reads "Waiting **on** one complete token-rate window" (`next-observed.js:234`).

## What this means for DRC-4589

Narrower than this issue claimed when filed, and worth stating precisely because the filed wording
overstates it.

DRC-4589's absence-stamp criterion is carried by `BothWithheldFiguresCarryTheAbsenceStampTest`
(`tests/test_next_delegation.py:798`, on `spacedock-ensign/drc-4589`), which has **two** positive
arms. One calls `nextProjectDelegation` directly, and its own docstring already records that the
function has no caller. The other calls `renderNext()` against the rail — the emitter a reader does
meet. So it is not the case that the criterion was verified against a function no reader sees; half
of it was verified against the right one.

The real gap is that **neither arm routes through the Console panel.** `RAIL_FIXTURE` replaces
`nextProjectView` with a direct call to `nextProjectRail`, so the `renderNext()` arm never enters
`nextCockpitPanel`. Both arms would still pass if the Console branch stopped emitting
`nextProjectRail(context)`. The correction owed to DRC-4589 is that sentence, dated, not a silent
repoint.

## The solution

In this order, because the order is what keeps the expensive half visible.

1. Record the trace above as the answer to "what renders it" — done here, and already backed by two
   tests on `main`.
2. Add one assertion that the stamped absence renders on the **routed** Console panel: set
   `nextRoute` to the Console tab, call `renderNext()` with no stub over `nextProjectView` or
   `nextProjectRail`, and assert the stamped markup inside the panel. This is what replaces the
   coverage the next step deletes.
3. Delete `nextProjectDelegation`, `nextDelegationRateMarkup`, and the direct-call arm that is the
   only thing referencing either. Recompute the byte pins.
4. Post the dated correction on DRC-4589 naming what its criterion actually stood on, and naming
   the routed assertion that now carries it.

Rejected: deleting on the caller count alone, which is what this issue was filed to prevent — it
closes the cheap half and leaves no trace of the expensive one. Also rejected: wiring the card back
up. `6c7bd9c` replaced it deliberately with a rail that renders the same absence from the shared
derivation model, and two cards printing the same figure from two derivations is the disagreement
that refactor existed to end.

## Ordering

Blocked by DRC-4589. The test being deleted and the `data-next-absent` stamp at `:182` exist only on
that branch, and `next-delegation.js` is under `cargento_runtime/web/`, which AGENTS.md restricts to
one in-flight PR.

## History — superseded 2026-09-17 at triage

Three claims in the body as filed on 2026-09-17 were checked against `84d27a5` and do not hold. They
are kept because the rest of the report is sound and a later reader needs to know which parts were
retired.

1. **"has not [had a caller] since `8d2585c`"** — false. The call site is present at `8d2585c` and at
   its parent; `6c7bd9c` (#314) dropped it. Nine days, not sixteen.
2. **"DRC-4589's AC-2 was verified against a function that is not the one a reader sees"** —
   overstated. That criterion has two arms and one of them is on the reachable emitter. The defect
   is that neither arm routes through the Console panel; see **What this means for DRC-4589**.
3. **"Five of those have now surfaced here, and every one was green and mutation-checked"** — not
   checkable. No list of the five is on file and this triage did not reproduce the count.

The line span `next-delegation.js:168-186` in the original body is `168-195` on `84d27a5`. The
original's "It is stamped and unit-tested" is true only of `spacedock-ensign/drc-4589`; on `main`
the function has no test.

## Provenance

Filed 2026-09-17 from DRC-4589's implementation, which found it while verifying its own acceptance
criterion rather than while reading the file, and reported it instead of quietly satisfying the
criterion by direct call.
````

## Re-verification at `d9059ae` (triage cycle 2)

Everything above was derived at `84d27a5`. This pass re-derived it at
**`d9059ae3f0e2f73f40977bf5315afea994a56b3e`** — `docs(burndown): record what the Clean and Cogent
UI/UX burndown measured (#363)`, the current `origin/main` tip. `git diff --stat 84d27a5 d9059ae`
is one file, `docs/roadmap-burndown/README.md`, +488 lines. No runtime or test file moved, so every
line number and byte figure above carries forward unchanged. **`d9059ae` is the SHA every figure in
this entity is now derived against**, and each was re-run rather than copied.

Confirmed at `d9059ae`, each by its own command:

* `git grep -n nextProjectDelegation d9059ae -- .` returns exactly one line, its definition at
  `next-delegation.js:168`. No runtime caller, no test.
* The occurrence count over `cargento_runtime/web/` walked commit by commit across
  `8d2585c..6c7bd9c`: **every** commit in that range reads 2 until `6c7bd9c`, which reads 1. So
  `6c7bd9c` is the exact commit that dropped the caller, not merely the first later commit that
  lacks it.
* The seven-hop trace re-run hop by hop: `next-project.js:397` → `next-cockpit.js:3355` → the
  `else` branch at `:3332-3342`, whose `:3341` emits `nextProjectRail(context)` →
  `next-delegation.js:284` → `:290` → `nextRailDelegation` at `:202` → the `!metric.pctKnown`
  branch at `:207-209`, printing `metric.pctText` from `next-observed.js:221`.
* `nextDelegationRateMarkup` (`:159`) has one caller, `:193`, inside the dead card.
  `nextDelegationTrendMarkup` (`:151`) has two, `:186` (dead card) and `:214` (rail).
* `next-delegation.js` is 14,508 bytes at `d9059ae`, which is exactly the figure pinned at
  `tests/test_next_page.py:675`. The oracles are current against this tip.

**Four figures in the pass above are corrected here.** None changes a conclusion.

1. `test_v2_surfaces_mount_in_their_cockpit_panels_once` is defined at `test_next_cockpit.py:795`,
   not 833. `:833` and `:840` are two of its assertions, which is what the pass above was pointing
   at.
2. The absent-string assertion at `:840` is `self.assertNotIn("DELEGATION ·", ...)` — no trailing
   space. The pass above wrote `"DELEGATION · "`, which is not the literal in the file.
3. `RAIL_FIXTURE` spans `test_next_delegation.py:9-29`, not `:9-21`; the
   `nextProjectView = () => nextProjectRail(...)` assignment that removes the route is at `:27`.
4. The stylesheet claim was asserted rather than measured. Measured here by deleting lines 158-195
   from a copy of the file and recounting every `.next-delegation*` selector `styles.css` declares:
   uses go `.next-delegation` 6→2, `-withheld` 4→2, `-figure` 2→1, `-metrics` 2→1, `-caption` 2→1,
   `-trend` 2→2. **None reaches zero, so no selector orphans and `styles.css` does not change.**
   The seven `data-next-delegation*` attributes likewise all survive at ≥1.

Two further facts this pass established that the pass above did not have:

* **The deletion is 2,156 bytes.** Lines 158-195 of `next-delegation.js` (both functions plus the
  blank lines that separate them) total 2,156 bytes; the file goes 14,508 → 12,352. That is the
  floor of the assembled-page delta, and it is a *delta*, not an absolute — the absolute must be
  recomputed from the merged tip this work starts on, which is not `d9059ae`.
* **PR #362 has moved past the commit DRC-4589 is stacked on.** #362's head is `333d478`;
  `spacedock-ensign/drc-4589`'s tip `b9642e3` sits on `2a07380`, an earlier #362 commit, and its
  merge base with `main` is `6702fb5` (v0.26.0) — behind both `84d27a5` and `d9059ae`. DRC-4589
  rebases before this issue can start from it.

The live Linear surfaces were re-fetched this pass and match the captures above byte for byte:
DRC-4610's `updatedAt` still equals its `createdAt` (`2026-09-17T14:02:41.322Z`), so nothing has
been written to it, and the milestone description is unchanged. The captures above stand as the
pre-edit record.

DRC-4589's live body confirms the inversion this issue turns on. Its DELEGATION bullet reads:
`next-delegation.js:182 (Console tab) and next-delegation.js:209 via next-observed.js:221's pctText
(project rail)`. The same sentence is in its triage entity at `drc-4589/index.md:197`, and its AC-2
at `:314-322` names "both paths (next-delegation.js:182 and :209)" without tab labels. `:182` is the
card no route reaches; `:209` is the Console tab. DRC-4589's relations today carry
`relatedTo: DRC-4610` and an empty `blockedBy`.

## Acceptance criteria

- **AC-1 — offline:** The rewritten DRC-4610 body names `nextRailDelegation` as the emitter of the Console tab's absent-delegation figure and carries the hop-by-hop trace to it, every hop a runtime call site rather than a test. **Verified by:** each published hop re-run as `git grep -n <symbol> <merged tip> -- cargento/skills/cargento/cargento_runtime/web/` and returning a caller outside `tests/`; at `d9059ae` the hops are `next-project.js:397`, `next-cockpit.js:3355`, `:3341`, `next-delegation.js:284`, `:290`, `:202`, `:207-209`, with the string itself from `next-observed.js:221`. **Falsified by:** any hop in the published trace whose only reference outside its own definition lives under `cargento/skills/cargento/tests/`.
- **AC-2 — offline:** `nextProjectDelegation` and `nextDelegationRateMarkup` are gone from the repository, and `nextDelegationTrendMarkup` is not. **Verified by:** `git grep -nE 'nextProjectDelegation|nextDelegationRateMarkup' -- cargento/` returns nothing, while `git grep -n nextDelegationTrendMarkup -- cargento/` still returns its definition and the rail's call (`:214` at `d9059ae`, the surviving one of its two callers). **Falsified by:** either dead symbol surviving with test-only references, or `nextDelegationTrendMarkup` deleted alongside them because it shared the block.
- **AC-3 — offline:** The delegation absence stamp is asserted on the Console panel through the real route, with no stub standing in for the view. **Verified by:** a test that sets `nextRoute = {view:"project",project:"cargento",tab:"console"}`, calls `renderNext()` without assigning `nextProjectView` or `nextProjectRail`, and asserts the stamped absence inside `data-next-cockpit-panel="console"`; removing `+ nextProjectRail(context)` from `next-cockpit.js:3341` turns it red. **Falsified by:** the new assertion reaching the rail through a replaced view function the way `RAIL_FIXTURE` does at `test_next_delegation.py:27`, which would pass with `:3341` deleted.
- **AC-4 — offline:** DRC-4589's published tab labels are corrected and dated, and the record says what its two test arms actually stood on. **Verified by:** DRC-4589's Linear body naming `next-delegation.js:209` as the Console tab and `:182` as the card no route reaches, plus a dated note that neither arm of `BothWithheldFiguresCarryTheAbsenceStampTest` entered `nextCockpitPanel` — one being a direct call and the other a `renderNext()` over a replaced `nextProjectView`. **Falsified by:** the two labels swapped with no note, leaving no trace that the criterion had been verified off the rendered route.
- **AC-5 — offline:** `styles.css` does not appear in the diff, and every frontend byte pin is regenerated from the assets rather than reasoned to. **Verified by:** `git diff --stat` carrying no `styles.css` row and `tests/test_next_page.py:706`'s stylesheet digest unmoved, while all seven pinned values are recomputed — the per-part pair at `test_next_page.py:675-676`, the assembled length and digest at `:710` and `:713`, the same two at `test_next_flag.py:67` and `:69`, and the assembled digest at `test_focus.py:1024`. **Falsified by:** any one of the seven left at its old value, or a pin changed to a number no run produced.
- **AC-6 — offline:** The issue as it lands no longer asserts the three claims this triage retired. **Verified by:** the published body naming `6c7bd9c` rather than `8d2585c` as the commit that dropped the caller, stating the DRC-4589 charge as "neither arm routes through the Console panel" rather than "verified against a function no reader sees", and carrying no count of prior guards found in this milestone. **Falsified by:** any of the three surviving in the body without the History section that retires it.

### Expected surface and tolerance

Line figures are against `d9059ae` and are a floor: this work starts from a tip that includes
DRC-4589, which edits `next-delegation.js` and all three byte-pin oracles.

| Surface | Files | Lines | Tolerance |
|---|---|---|---|
| Runtime deletion | `cargento/skills/cargento/cargento_runtime/web/next-delegation.js` | −38 (lines 158-195; −2,156 bytes; file 14,508 → 12,352) | ±6 lines. Any deletion outside 158-195 is out of scope. |
| Routed assertion | `cargento/skills/cargento/tests/test_next_cockpit.py` | +12 to +25 | ±10. A second new test file is out of tolerance — the routed fixture already lives here. |
| Test deletion | `cargento/skills/cargento/tests/test_next_delegation.py` | −20 to −30 (the direct-call arm; `:814-840` on `spacedock-ensign/drc-4589`, 27 lines with its docstring, plus the class docstring's "both emitters" sentence and likely the class name) | ±10. The two surviving arms at `:842` and `:856` must not move. |
| **Frontend byte-pin oracles** | `tests/test_next_page.py` (`:675`, `:676`, `:710`, `:713`), `tests/test_next_flag.py` (`:67`, `:69`), `tests/test_focus.py` (`:1024`) | 7 values, all regenerated | **Zero tolerance.** Exactly 7 changed values, none hand-derived. Costed as its own row because the diff reaches `cargento_runtime/web/`. |
| Stylesheet | `cargento/skills/cargento/cargento_runtime/web/styles.css` | 0 | **Zero.** Measured: no `.next-delegation*` selector orphans. Any change here means the deletion took something it should not have. |
| Linear | DRC-4610 title and body; DRC-4589 body correction; milestone comment | not a repo diff | Three writes, all at implementation. No fourth. |

### Conflict surface and ordering

**Conflict surface: `cargento/skills/cargento/cargento_runtime/web/next-delegation.js`**, plus
`tests/test_next_cockpit.py` and the three byte-pin oracles.

* **Blocked by DRC-4589**, and the block is load-bearing rather than procedural. The
  `data-next-absent` stamp that AC-3 asserts does not exist on `main` — `git grep data-next-absent
  d9059ae` finds it nowhere in `next-delegation.js`; it exists only at `:182` and `:209` on
  `spacedock-ensign/drc-4589`. The test arm AC-2 deletes exists only there too. Starting before
  DRC-4589 lands means asserting a stamp that is not in the tree.
* **DRC-4589 is not yet a PR.** It is a local worktree branch at `b9642e3`, unpushed, stacked on
  `2a07380` — an earlier commit of PR #362, whose head has since advanced to `333d478`. Its merge
  base with `main` is `6702fb5`, behind `84d27a5` and `d9059ae`. So the order is: #362 merges,
  DRC-4589 rebases onto that tip and merges, and **implementation for this issue starts from the
  merge commit that lands DRC-4589** — not from `d9059ae`, and not from `spacedock-ensign/drc-4589`
  as it stands.
* **PR #362 (DRC-4588 + DRC-4590) does not touch `next-delegation.js`.** Its 16-file list has no
  such row. It does touch `next-cockpit.js`, `test_next_cockpit.py`, and all three byte-pin
  oracles, so it contends on four of this issue's five code surfaces even though it contends on
  none of the deletion itself.
* **DRC-4602 does not contend.** It is queued for `styles.css`, and this issue's stylesheet row is
  measured at zero. The only thing that still serialises them is AGENTS.md's one-in-flight-PR rule
  over `cargento_runtime/web/`.
* **A conflict here is the kind AGENTS.md warns about specifically:** two branches that both change
  a web asset produce byte pins each correct for a tree that no longer exists. Recompute all seven
  from the assets on the merged tip. Do not resolve them textually.

### Gate asks

1. **Delete, or wire back?** This triage recommends delete. `6c7bd9c` replaced the card with a rail
   that renders the same absence from the shared derivation model, and two cards printing one figure
   from two derivations is the disagreement that refactor existed to end. Wiring it back reopens it.
2. **May this issue write to DRC-4589's Linear body?** AC-4 needs it. The inverted `(Console tab)` /
   `(project rail)` labels are published there and in `drc-4589/index.md:197`, and the correction is
   worthless if it lands only on DRC-4610. Ask covers both surfaces.
3. **One PR with DRC-4589, or its own PR after it?** AGENTS.md says one PR per conflict surface, not
   one per issue, and these two share `next-delegation.js`, `test_next_delegation.py` and all three
   oracles. Folding DRC-4610 into DRC-4589's PR saves a review, a CI cycle and one merge
   serialisation, and lets the byte pins be computed once. Against it: DRC-4589 is already large
   (20 files, +1,994/−294) and is itself stacked. This triage has no preference strong enough to
   choose for the gate.
4. **Set `blockedBy: DRC-4589`?** Today the relation is `relatedTo` in both directions, which does
   not stop anyone picking this up. The dependency is real per the section above.
5. **Does the title change?** The current title names `8d2585c`, which this triage falsified. The
   drafted replacement names `6c7bd9c` and the two unrouted tests. A title change breaks any
   external reference to the old wording; there appear to be none.
6. **Milestone description: edit, or leave to `sync-project`?** Draft below is one sentence into
   *How this was measured*. This triage did **not** touch the "Fifteen issues" count: the milestone
   reports 16.67% progress and this pass did not derive the issue set behind that figure, and a
   count carried rather than derived is the exact error the burndown has already made three times.
   Counting is `sync-project`'s job, not this issue's.

### Drafted milestone comment bullet

Appended to the existing `Read before building` comment (id `38cca5e4-fb02-46e9-8fb0-3aa6c57c9c3e`,
posted 2026-09-17T11:18:20Z) rather than rewritten into it — `save_milestone` resends the whole
body and has no patch operation, which is the reason that comment exists at all.

````text
* A test that renders through a replaced view function is not a routed test. `RAIL_FIXTURE` in
  `test_next_delegation.py` assigns `nextProjectView = () => nextProjectRail(...)`, so every arm
  built on it would still pass if the Console panel stopped emitting the rail. Two criteria in this
  milestone were checked that way. When an AC says a reader sees something, set `nextRoute` and
  call `renderNext()` with nothing stubbed between the route and the markup, and name the line
  whose deletion turns the assertion red. See DRC-4610.
````

### Drafted milestone description edit

One sentence, appended to the end of the first paragraph of **How this was measured**. Nothing else
in that description changes.

````text
One caution about this milestone's own evidence: a criterion here was checked against a function no
route renders, and a second against the right function through a fixture that had removed the
route — neither was visible while the tests were green. DRC-4610 records the trace and the
correction, and no criterion in this milestone should rest on an assertion that does not name the
line whose deletion turns it red.
````

### Summary

`nextProjectDelegation` is dead and the Console's absent-delegation figure comes from
`nextRailDelegation` instead, by a seven-hop trace in which every hop is a runtime call site. The
issue as filed names the wrong commit — `6c7bd9c` dropped the caller, not `8d2585c`, proven by
walking the occurrence count commit by commit across the range — and overstates the charge against
DRC-4589, whose criterion has two arms with one on the reachable emitter. What survives is the
finding worth keeping: **neither arm routes through the Console panel**, so both would pass if the
panel stopped emitting the rail at all, and DRC-4589 publishes the two emission sites with their tab
labels inverted. A second function, `nextDelegationRateMarkup`, dies with the card; a third,
`nextDelegationTrendMarkup`, does not. The stylesheet was measured rather than assumed and does not
change. Six acceptance criteria, seven byte pins with zero tolerance, blocked on DRC-4589 — which is
an unpushed branch stacked on a commit PR #362 has already moved past — and six decisions for the
gate, the largest being whether this ships inside DRC-4589's PR or behind it.

## Stage Report: triage

- DONE: Pre-edit Linear record re-fetched and confirmed current before any drafting
  DRC-4610's `updatedAt` still equals its `createdAt`, and the milestone description matches the
  capture byte for byte, so the existing `## Linear edits made` captures stand unmodified as the
  record; the DRC-4610 title and body drafts were already beside them.
- DONE: Drafts written for every Linear surface this issue touches
  The milestone half was missing — the prior pass promised a comment bullet "below" and wrote none.
  Added **Drafted milestone comment bullet** and **Drafted milestone description edit**, and
  recorded in Gate asks why the issue count was deliberately left alone.
- DONE: Six criteria written in the scanned bullet shape
  Six criteria under `## Acceptance criteria`, all hyphenated, all `offline`, each bold label
  opened and closed on its own line. None needs a live board: every one is a grep, a diff row or a
  test run.
- DONE: Surface table declared with the byte-pin oracles costed separately
  Six rows. The three oracles are their own row at zero tolerance with all seven pinned values
  enumerated by file and line, kept apart from the runtime deletion row, because the diff reaches
  `cargento_runtime/web/`.
- DONE: Every figure re-derived against the named tree, and the SHA recorded
  `d9059ae3f0e2f73f40977bf5315afea994a56b3e` is recorded in `## Re-verification at d9059ae`, which
  also shows it differs from the prior pass's `84d27a5` by one docs file. Nothing was carried:
  the call graph was re-grepped hop by hop, the caller loss was bisected commit by commit across
  `8d2585c..6c7bd9c` rather than sampled at the endpoints, and the byte figures were measured from
  the file. Four figures in the prior pass were wrong and are corrected in place.
- DONE: Stylesheet composed rather than censused
  The prior pass asserted no CSS orphans. This pass deleted lines 158-195 from a copy and recounted
  every `.next-delegation*` selector `styles.css` declares against the surviving JS: six selectors,
  lowest surviving use 1, none zero. The zero-tolerance stylesheet row rests on that, not on a
  per-rule read.
- DONE: Conflict surface and block named with the tips they depend on
  `next-delegation.js` plus `test_next_cockpit.py` and the three oracles. Blocked by DRC-4589 for a
  load-bearing reason, not a procedural one. PR #362 does not touch `next-delegation.js`; DRC-4602
  does not contend at all. Recorded that DRC-4589 is an unpushed branch whose base `2a07380` is
  behind #362's head `333d478`, and that implementation starts from the merge commit landing
  DRC-4589.
- DONE: Gate asks and summary written
  Six asks under `### Gate asks`, the load-bearing ones being the delete-versus-rewire decision, the
  write to another issue's body, and whether this rides inside DRC-4589's PR.
- DONE: Nothing written to Linear
  No `save_issue`, `save_milestone` or `save_comment` call was made. Reads only: `get_issue` on
  DRC-4610 and DRC-4589, `get_milestone`, and one `list_issues`.

### Summary

Completed a triage the previous cycle left unfinished at the drafts, and re-derived it against
`d9059ae` rather than trusting the `84d27a5` figures it inherited. The substance held — the dead
card, the inverted tab labels and the unrouted test arms are all real — but four of the prior pass's
line and string figures were wrong, the stylesheet claim was asserted rather than measured, and the
promised milestone draft did not exist. Added six acceptance criteria, a six-row surface table with
the seven byte pins costed separately at zero tolerance, the ordering constraint that DRC-4589 is an
unpushed branch behind PR #362's current head, and six gate asks.
