---
id: drc-4032
title: 'D9 · Quiet hours'
status: triage
source: https://linear.app/recce/issue/DRC-4032
started: "2026-09-16T09:27:00Z"
completed: ""
verdict: ""
score: 0.5
worktree: ""
issue: ""
pr: ""
mod-block: ""
linear-status: 'In Progress'
milestone: 'Spend attention well'
release: 'r3'
estimate: 'XS'
reconciled: ""
move: extend
promise: P2
---

[DRC-4032](https://linear.app/recce/issue/DRC-4032) : Linear priority Low, estimate XS.

## Problem

A person protecting a block of deep work or sleep needs Cargento to suppress notifications during configured hours instead of pinging them for every state change. Currently, notification popups (`maybe_popup`, `handle_payload`, `unasked._raise`, `tripwires.collect`) and off-machine reach nudges (`reach.maybe_reach_nudge`) fire whenever transitions occur, without respecting user-configured quiet hours.

## Proposed approach

1. Add settings surface for quiet hours:
   - CLI flag: `--quiet-hours HH:MM-HH:MM` (e.g. `22:00-08:00` or `13:00-15:00`)
   - Rollback/disable CLI flag: `--no-quiet-hours`
   - Env var: `CARGENTO_QUIET_HOURS`
   - Stored setting file: `~/.cargento/quiet_hours` (read if no CLI flag or env var is set)
2. Add parsing and validation helper `parse_quiet_hours` in `notifications.py`.
3. Add `is_quiet_hours(config, *, now=None)` in `notifications.py` checking local time against the window (supporting windows spanning midnight like `22:00-08:00`).
4. In `notifications.py`:
   - `maybe_popup`: if `is_quiet_hours(config, now=now)`, suppress popup and return early.
   - `handle_payload`: if `is_quiet_hours(config, now=now)`, do not fire popup.
   - `maybe_ask_popup`: DO NOT suppress. Direct questions must still fire.
5. In `unasked.py` (`_raise`), `tripwires.py` (`_collect`), and `reach.py` (`maybe_reach_nudge`): suppress raises/nudges during quiet hours.
6. In `lifecycle.py` (`spawn_argv`): forward `--quiet-hours` and `--no-quiet-hours` to daemon child.
7. Unit tests in `test_notifications.py`, `test_cli.py`, `test_lifecycle.py`, and `test_reach.py`.

## Linear edits made

### Original issue description
```markdown
## User value

A person protecting a block of deep work notices this once Cargento can suppress every notification except a direct question during hours they choose, instead of pinging them for every state change regardless of the time. P2, extend: a quiet-hours window is a new clause on the notification promise, which today has no way to hold back anything but a direct ask.

## What needs to be done

Add a settings surface for a quiet-hours window. During quiet hours, suppress every notification except a direct question that needs the user's answer.

## Scores (blind-panel medians)

| Impact | Risk-adjusted impact | Access | Build | Detector risk |
| -- | -- | -- | -- | -- |
| 34 | 31 | 25 | 15 | 3 |

Score legend: see the Visibility 2x2 board README.
```

### Pre-edit milestone description
Captured from milestone `Spend attention well` (Linear ID `70f7bd70-5210-438d-885c-ff6af52a3363`).

## Expected surface and tolerance

Files modified: `config.py`, `cli.py`, `lifecycle.py`, `notifications.py`, `unasked.py`, `tripwires.py`, `reach.py`, and tests (`test_notifications.py`, `test_cli.py`, `test_lifecycle.py`, `test_reach.py`).
Runtime delta: ~120 lines across 7 files. Tests delta: ~150 lines.

## Acceptance criteria

1. (offline) `--quiet-hours 22:00-08:00` parses and validates standard 24-hour time windows including windows spanning midnight. Verified by: unit test in `test_notifications.py`. Falsified by: `22:00-08:00` failing to parse or misclassifying 23:00 / 02:00.
2. (offline) During quiet hours, state transitions in `maybe_popup`, Claude hook notifications in `handle_payload`, unasked departures, tripwire conditions, and reach nudges are suppressed. Verified by: unit tests with injected timestamp. Falsified by: popup or reach attempt occurring during active quiet hours.
3. (offline) During quiet hours, direct questions (`maybe_ask_popup`) continue to fire popups without suppression. Verified by: unit test asserting `maybe_ask_popup` outcome during quiet hours. Falsified by: question popup being suppressed.
4. (offline) `--no-quiet-hours` disables quiet hours even if env var or state file is present. Verified by: unit test in `test_cli.py`.
5. (interactive/offline) CLI daemon respawn forwards `--quiet-hours` and `--no-quiet-hours` in `spawn_argv`. Verified by: unit test in `test_lifecycle.py`.

## Test plan

1. Unit tests for `parse_quiet_hours` and `is_quiet_hours` covering within-day, overnight/spanning midnight, edge boundaries (start minute, end minute), and invalid inputs.
2. Unit tests for `maybe_popup`, `handle_payload`, and `maybe_ask_popup` during quiet hours vs outside quiet hours.
3. Unit tests for reach and unasked suppression during quiet hours.
4. Full validation suite: ruff, mypy, validate_plugins, unittest discover, coverage report.

## Review depth

Medium (Standard): local pre-PR checks, TDD implementation, sync-docs.

## Out of scope

GUI schedule picker inside browser DOM (DRC-4032 specifies settings surface, implemented via CLI/env/file and daemon forwarding).
