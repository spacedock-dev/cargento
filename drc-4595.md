---
id:
title: "Put Console's operations first and lift the steer box out of TRIPWIRES"
status: triage
source: "https://linear.app/recce/issue/DRC-4595/put-consoles-operations-first-and-lift-the-steer-box-out-of-tripwires"
started: 2026-09-17T10:47:50Z
completed: ""
verdict: ""
score: 0.6
worktree: ""
issue: ""
pr: ""
mod-block: ""
linear-status: "Backlog"
milestone: Clean and Cogent UI/UX
release: ""
promise: "P2"
move: "sharpen"
estimate: ""
reconciled: ""
gates:
    version: 1
    records:
        - id: gate:drc-4595:triage
          stage: triage
          attempts:
            - id: gate-attempt:drc-4595-triage-1
              briefing:
                id: briefing:drc-4595:triage:attempt-1:revision-1
                digest: sha256:720c6d3f6d8db629e83203955eb1d5345f96ce307917bb0a66260053d948a171
                room-ref: ./drc-4595/review/triage/briefing-1
---

[DRC-4595](https://linear.app/recce/issue/DRC-4595/put-consoles-operations-first-and-lift-the-steer-box-out-of-tripwires) — Put Console's operations first and lift the steer box out of TRIPWIRES

Seeded 2026-09-17 from the live Linear read of the Clean and Cogent UI/UX milestone.
Linear owns the current issue body, its relations and its resources; triage fetches them
live and validates them against the tree before anything is built. No triage, approval,
implementation or delivery is claimed here.

---

# Triage — 2026-09-17

**Still real.** Every structural claim in the issue reproduces on the tree the work will land on.
Every quantitative claim taken from the annotated capture has moved, and one did not reproduce at
all. Re-measured live rather than re-read.

**Measured against `spacedock-ensign/drc-4587` @ f6ced2f8, not `main`.** The PR sequence puts this
issue in PR 6, so DRC-4587's type scale is already underneath it. Measuring on `main` would have
quoted a 12.5px sentence floor that no longer exists and five byte pins that have already moved.

**How the live figures were taken, so a stranger can retake them.** `python3 server.py --port 4597`
from `cargento/skills/cargento` on that worktree; Chrome at
`#n=project:recce%2Fcargento:claude%3A7985111d:console`; viewport 1680×1057, DPR 1; heights read
with `getBoundingClientRect()` off `[data-next-cockpit-panel="console"]` and its element children.
Pixel heights are viewport- and data-dependent, so the share of the panel is the figure that
travels, not the pixel count.

## Linear edits made

Nothing has been written to Linear. Both captures below are the pre-edit record; both drafts are
for `implementation` to write **after** the gate approves them. Captures are fenced so the entity's
own heading tree stays readable — the text inside is byte-for-byte what Linear holds.

### Captured original — issue body, verbatim (DRC-4595, read 2026-09-17)

````text
## User value

Anyone who opens Console to act notices this. Today the panel leads with a disabled terminal and a configuration paragraph, and the box for writing a note sits under an unrelated heading.

## The Problem

Console opens with 317px of setup documentation about capabilities that are switched off — 43% of a 732px panel before the first operational heading. EXACT SESSION TERMINAL sits at y=134 with its paragraphs at y=172-250, the observer-model line at y=295 and Raw project status at y=343; DELEGATION does not appear until y=397, and the live figures (8% and 1%, the two meters, TRIPWIRES, STEER) all render below y=500. Setup prose is constant; operations change minute to minute.

Worse, the one input surface on the page is the last element of the panel, at y=691 — and it is not even a peer of the operations panels: `nextProjectRail` reaches it through `nextProjectGuardrails(project, state, true)`, so it renders *inside* TRIPWIRES, a section captioned "local only · nothing enforces these". Its placeholder reads "Tell this project what to do next" and its submit says `send`, but submitting only records a local draft; the correction arrives after the press.

**Measured**

* Console capture: EXACT SESSION TERMINAL y=134, paragraphs y=172-250, observer model y=295, Raw project status y=343, DELEGATION y=397, panel body from y≈80 — 317px of 732px, 43.3%
* next-cockpit.js:3332-3341 orders console scope, terminal surface, observer-model controls, console status, then `nextProjectRail`
* The steer surface reaches the page only through `nextProjectRail` → `nextProjectGuardrails(project, state, true)` → `nextProjectSteer` (next-controls.js:111), inside the TRIPWIRES section
* STEER · LOCAL ONLY renders at 10px mono ink3 (styles.css:330); the placeholder is at next-controls.js:123 and the send button at :127
* The receipt "Draft recorded in this tab. Not delivered. Cargento has no write path into a session." is rendered only after submit (next-controls.js:112-116)
* The steer input's cap is 500 characters (next-controls.js:123)
* project.js:790-791 shows the registration recipe for four of the eight published reasons
* Console has 5 interactive elements; Now and Decisions have 0 and Course 1

**In the attached screenshot**

1. 317px of setup prose before DELEGATION at y=397
2. Four CLI flags, zero controls, nothing to copy
3. STEER input at y=691, last row, inside TRIPWIRES
4. "Tell this project what to do next" delivers nothing

## The Solution

Invert the Console branch: scope header, then the select-a-session prompt when unfocused, then `nextProjectRail(context)`, then one `<details class="next-cockpit-console-setup">` holding the terminal surface, the observer-model controls and the console status.

Keep the scope header and the prompt **outside** the disclosure — the first says whose console this is and the second is the only instruction at project scope, so collapsing them leaves a project-scope Console with no way in. Derive the summary from the same flags the collapsed sections render from ("How this server was started — terminal bridge off, observer model off"), and when either capability is on, render that section expanded outside the disclosure, because an enabled terminal is operational content.

Promote the steer composer by calling the existing `nextProjectSteer(project, state)` from the cockpit shell immediately before the tab list and passing `includeSteer=false` to the guardrails call — do **not** retype the markup, or you lose `data-next-steer-form`, `data-next-draft`, `data-next-controls-project`, `data-next-focus` and the 500-character cap. Give the promoted instance a bar layout class.

Fix the pre-submit copy: placeholder "Draft a next step — kept in this tab only", and replace the 10px header with a label plus a sentence at the sentence tier in ink2: "Cargento has no write path into a session. Anything you type here is a note to yourself, kept in this browser tab." Style the submit as plain `.next-action`, never primary.

Flag the `send` → `save draft` rename to whoever owns NUI-10 rather than shipping it, since that ruling keeps the control specifically to demonstrate the interaction shape.

## Acceptance

- [ ] Console's first screenful is DELEGATION, WAITING ON YOU, CAPACITY and TRIPWIRES; setup prose is one summary row
- [ ] The scope header and the project-scope select-a-session prompt render outside the disclosure
- [ ] The summary's on/off clauses are derived from the same flags the collapsed sections render from, and an enabled capability renders expanded
- [ ] The steer composer renders once, in the chrome, through the existing renderer with every `data-` attribute and the 500-character cap intact
- [ ] The local-only warning is a sentence at or above the sentence floor, visible before the first keystroke
- [ ] The send/save-draft rename is filed against NUI-10 rather than made in this change

---

Complaint **C3** · tabs: console · severity **major** · effort **M** · blocked by DRC-4590, DRC-4591

Raised from user feedback; mechanism and figures established by a measured audit of the live board and the shipped stylesheet.
````

### Captured original — milestone description, verbatim ("Clean and Cogent UI/UX", read 2026-09-17)

````text
## The user value

**The board can be read at a glance: you can tell a label from its answer, a figure from a gap, and the one thing to do on a screen from the prose around it.**

Users report low contrast. Every text ink in the cockpit passes WCAG AA — ink3, the dimmest, is 5.67:1 on panel, nearer AAA than the AA floor. Measured size-aware with APCA, the same inks fail badly at the sizes they are actually set in: at 12.5px/400 even `#f4f1e8`, the brightest ink in the palette, scores Lc 98.0 against a body-text requirement of 100, and the 9.5px and 10px label steps fall off the bottom of the lookup table entirely.

So there is no colour left to spend and no contrast fix available. The work is to raise the sentence tier, collapse the label tier, and give label, value and absence three distinct registers instead of the one dim ink that currently carries 89% of the characters on Held to.

## What is left

Twelve issues. Three are foundations and everything else depends on them:

* **Raise board sentences to a 15px tier** and collapse six sub-12px tokens into one label tier.
* **Split the three inks onto label, value and absence roles**, so a label stops sharing its ink with its own answer.
* **Always render the Held to reading control**, disabled with its reason, instead of deleting the tab's only verb exactly when a newcomer looks for it.

Then: the briefing, the tab strip, a control primitive with one primary action per tab, caveat tiering, Held to ordering, Console ordering, the scope rail, the timeline filter, and a guardrail so the asset test enforces size rather than the contrast that already passes.

## Waits on

Nothing outside this milestone. `type-scale` and `ink-roles` gate most of the rest; `ink-roles` itself waits on `type-scale`.

## How this was measured

Contrast computed from the shipped tokens and independently recomputed during triage, matching to the decimal. APCA from the same pairs at the sizes they render at — **audit-only: no APCA implementation, table or fixture exists in the repository, so these figures cannot be reproduced from the tree.** [DRC-4596](https://linear.app/recce/issue/DRC-4596/make-the-asset-test-enforce-font-size-not-the-contrast-that-already) adds the guardrail that would, and lands after the change the figures justify. Density, ink distribution and control counts read off the live DOM at `127.0.0.1:4553`. Density figures are floors — they were taken on a quiet, unannotated session, and a busy project has more text, not less.

Eleven claims were raised and rejected rather than filed. The two worth knowing: brightening the inks cannot fix C1, and merging the three thin tabs is a route change plus an amendment to NUI-3, not a layout change.
````

### Drafted rewrite — issue body (NOT written to Linear)

Shorter than the original: 1,010 words → 680. Nothing was deleted; the superseded figures are dated
and demoted.

````text
## User value

Anyone who opens Console to act on a session sees this, every time they land there. The panel
spends its first third on setup documentation for two capabilities that are switched off, and the
one place on the page you can write anything sits last, inside a section captioned "local only ·
nothing enforces these".

Promise **P2 — see the gap while working**. Move: **sharpen**. Console keeps everything it shows;
only the order and one control's copy change.

## The problem

Measured 2026-09-17 on `spacedock-ensign/drc-4587` @ f6ced2f8 (the tree this lands on), project
`recce/cargento`, session-scoped Console, viewport 1680×1057.

* The Console panel is 968px tall. **DELEGATION — its first operational heading — starts 341px
  down, 35.2% of the way in.** Of that, 309px (31.9%) is EXACT SESSION TERMINAL (175px), the
  observer-model notice (71px) and Raw project status (63px).
* On a default run the terminal bridge and the observer model are both **off**, so those 309px
  describe capabilities the reader cannot use. They also never change while you work; the figures
  below them change minute to minute.
* `next-cockpit.js:3333-3341` emits scope header, terminal surface, observer-model controls,
  console status, then `nextProjectRail(context)` — the operations rail last.
* **The steer input starts 830px down the same 968px panel, 86% of the way in, and it is not a
  peer of the operations panels at all.** `nextProjectRail` (next-delegation.js:291) reaches it
  through `nextProjectGuardrails(project.key, state, true)`, so `nextProjectSteer` renders as the
  last child of the TRIPWIRES section (next-controls.js:168).
* Its placeholder promises delivery — "Tell this project what to do next" (next-controls.js:123) —
  and its submit reads `send ⏎` (:127). The correction ("Draft recorded in this tab. Not delivered.
  Cargento has no write path into a session.") is built only from `state.steers`, so it exists
  only **after** the press (:112-116).
* Before the press there is only `STEER · LOCAL ONLY` at 11px mono ink3 (`--fs-label`,
  styles.css:338), over a 12.5px sans input (styles.css:342). DRC-4587 set the board's sentence
  floor at 15px (`--fs-sentence`); nothing in this control carries the warning at that tier.

Ordering, not volume. Every block above stays on the page.

## The solution

**Console branch** (next-cockpit.js:3333-3341). Emit: scope header → the project-scope
select-a-session prompt when unfocused → `nextProjectRail(context)` → one
`<details class="next-cockpit-console-setup">` holding the terminal surface, the observer-model
controls and the console status.

Keep the scope header and the prompt **outside** the disclosure. The first says whose console this
is; the second is the only way into a session at project scope, so collapsing it leaves project
scope with no entry. Use `nextCockpitDisclosureAttr("console-setup")`, not a bare `<details>`, so
open/closed survives a redraw through the lane the cockpit already owns (next-cockpit.js:17,
:3360-3371). Derive the summary's clauses from the same flags the collapsed sections render from
("How this server was started — terminal bridge off, observer model off"), and when a capability is
**on**, render its section expanded and outside the disclosure: an enabled terminal is operational
content. The summary must read off the flags, not off whether the body came back non-empty —
`nextCockpitTerminal` returns `""` with no focus or no surface (:3311-3315) and
`nextCockpitConsoleStatus` returns `""` with no sessions (:3090).

**Steer composer.** Call the existing `nextProjectSteer(project, state)` from `nextProjectCockpit`
immediately before `nextCockpitTabList()`, and pass `includeSteer=false` at next-delegation.js:291.
Reuse the renderer — retyping the markup loses `data-next-steer-form`, `data-next-draft`,
`data-next-controls-project`, `data-next-focus` and `maxlength=500`. Give the promoted instance a
bar layout class. **Delete `nextProjectControls` (next-controls.js:171-175)**: it has no caller
anywhere in `web/` and it builds a second composer, so "renders once" cannot be proved while it
exists.

**Pre-submit copy.** Placeholder → "Draft a next step — kept in this tab only". Replace the mono
header with a label plus one sentence at `var(--fs-sentence)` in `--ink2`, carrying its own prose
line-height — that pair is DRC-4587's own test for what counts as a sentence: "Cargento has no
write path into a session. Anything you type here is a note to yourself, kept in this browser tab."
Style the submit as the plain, non-primary control class **DRC-4590** introduces; never primary.

**Not in this change.** The `send` → `save draft` rename is filed against NUI-10, which keeps this
control specifically to demonstrate the interaction shape.

## Acceptance

Full criteria, with their offline/interactive split and each `Verified by:` clause, live in the
burndown entity `drc-4595.md`. In brief:

- [ ] Console emits scope header, prompt, operations rail, then one setup disclosure — in that order
- [ ] The scope header and the project-scope prompt render outside the disclosure
- [ ] The summary's on/off clauses come from the same flags the collapsed sections render from, and an enabled capability renders expanded
- [ ] The composer renders exactly once, in the chrome, through the existing renderer, with every `data-` attribute and the 500-character cap intact
- [ ] The local-only warning is a sentence at the 15px `--fs-sentence` tier, present before any submit
- [ ] The setup disclosure's open/closed state survives a redraw
- [ ] The submit uses DRC-4590's non-primary control class (assessable only once DRC-4590 has merged)
- [ ] The send/save-draft rename is filed against NUI-10 rather than made here

---

### History — figures as filed, 2026-09-17 (superseded; kept as the record of what was believed)

Filed against `main` @6702fb5c, before DRC-4587 moved the type scale under it. Retriaged the same
day against `spacedock-ensign/drc-4587`. **Every structural claim held. These figures did not:**

* "317px of a 732px panel, 43.3%" → **341px of 968px, 35.2%**. Both are viewport-dependent; the
  share travels, the pixel count does not.
* The callout y-coordinates (y=134 / 172-250 / 295 / 343 / 397 / 691) → not re-derived. They are
  document coordinates of one capture and cannot be reproduced from the tree. Superseded by the
  panel-relative offsets above.
* "STEER · LOCAL ONLY renders at 10px mono ink3 (styles.css:330)" → **11px, `var(--fs-label)`,
  styles.css:338**. DRC-4587 collapsed the 9/9.5/10/10.5px steps into one 11px label tier.
* "Console has 5 interactive elements; Now and Decisions have 0 and Course 1" → measured
  **5 / 3 / 0 / 3** (`button, input, select, textarea` inside the panel) on a three-session
  project. Console's 5 reproduced; Now and Course did not. The count moves with how much a project
  has on it, so it is not acceptance-grade and no criterion rests on it.
* "a sentence at the sentence tier" meant 12.5px when this was filed. DRC-4587 raised the floor to
  **15px (`--fs-sentence`)**, which is what the warning is now held to.
* `project.js:790-791` (four of eight registration reasons) still resolves, but it argues for
  DRC-4591's caveat tiering rather than for this reordering. Left here rather than carried up.

Complaint **C3** · tabs: console · severity **major** · effort **M**. Raised from user feedback.
````

### Drafted rewrite — milestone description correction (NOT written to Linear)

The milestone is almost entirely still true. One clause is not, and it is the clause that vouches
for a figure this triage failed to reproduce. **Narrow correction: replace the sentence**

> Density, ink distribution and control counts read off the live DOM at `127.0.0.1:4553`. Density
> figures are floors — they were taken on a quiet, unannotated session, and a busy project has more
> text, not less.

**with**

> Density, ink distribution and control counts read off the live DOM at `127.0.0.1:4553`. Density
> figures are floors — they were taken on a quiet, unannotated session, and a busy project has more
> text, not less. Control counts are floors in the same way and are **data-dependent**: retaken on a
> three-session project during DRC-4595's triage, Console's 5 reproduced but Now and Course came
> back 3 and 3 against the 0 and 1 on file. They illustrate the imbalance; no acceptance criterion
> in this milestone may rest on one.

Nothing else in the milestone needs changing. "Console ordering" is still an accurate name for this
issue, the three foundations are still the three foundations, and the APCA audit-only caveat is
untouched by anything measured here.

### Labels to set

Already correct on the issue; `implementation` sets nothing new.

* `journey:mid-flight` — the reader is inside a session, acting, not starting or closing out.
* `move:sharpen` — the panel keeps every block it has. Order and one control's copy change.
* `Design`, `discovered-by-agent` — retained as filed.

## Acceptance criteria

- **AC-1 — offline:** the Console panel emits, in order, the scope header, then the project-scope select-a-session prompt when unfocused, then the operations rail, then exactly one setup disclosure. **Verified by:** a JS-harness test rendering the console panel and asserting `index('data-next-project-rail') < index('next-cockpit-console-setup')` and `index('next-cockpit-scope') < index('data-next-project-rail')`; today the first comparison is false because the rail is emitted last at next-cockpit.js:3341. **Falsified by:** moving any of the three setup sections back above `nextProjectRail(context)`.
- **AC-2 — offline:** the scope header and the project-scope select-a-session prompt render outside the setup disclosure, so project scope still has a way into a session when the disclosure is shut. **Verified by:** a harness test rendering the panel with no focus and asserting both `next-cockpit-scope` and `next-cockpit-empty` appear before the `<details class="next-cockpit-console-setup">` open tag, and neither appears after it. **Falsified by:** wrapping either inside the disclosure — the test then finds the prompt only after the `<details>` tag.
- **AC-3 — offline:** the disclosure summary's on/off clauses are derived from the same flags that decide whether each collapsed section renders, and a capability that is on renders expanded and outside the disclosure. **Verified by:** a harness test rendering twice from one fixture, flipping only the observer-model flag, asserting the summary text changes from "observer model off" to "observer model on" AND that the observer-model section leaves the `<details>` subtree in the second render. **Falsified by:** hard-coding either clause, or deriving it from whether the section's HTML came back non-empty — `nextCockpitTerminal` returns `""` with no focus (next-cockpit.js:3311-3315), so a body-derived summary would say "off" for an enabled bridge on an unfocused route.
- **AC-4 — offline:** the steer composer is constructed exactly once per render, in the cockpit shell rather than inside TRIPWIRES, through `nextProjectSteer`, and the rendered instance still carries `data-next-steer-form`, `data-next-draft`, `data-next-controls-project`, `data-next-focus` and `maxlength="500"`. **Verified by:** a harness test asserting `html.split('data-next-steer').length - 1 === 1`, that the composer's index is below `nextCockpitTabList`'s output and not within the `data-next-guardrails` section, and that all five attributes are present; plus `grep -c 'nextProjectSteer(' cargento_runtime/web/` returning exactly 2 (one definition, one call site) after `nextProjectControls` is deleted — it returns 4 today. **Falsified by:** retyping the markup at the new call site, or leaving `nextProjectControls` in place, which restores a second construction path a later caller would silently render.
- **AC-5 — offline:** before any submit, the composer shows a sentence stating there is no write path, set in sans at `var(--fs-sentence)` with its own prose line-height — DRC-4587's own test for what is on the sentence tier. **Verified by:** a harness test rendering `nextProjectSteer(project, {steers:[]})` and asserting the no-write-path sentence is present with an empty receipt list, plus a `styles.css` assertion that its selector declares `font-size:var(--fs-sentence)` and a `line-height`; today that sentence exists only in the post-submit receipt built from `state.steers` (next-controls.js:112-116). **Falsified by:** leaving the warning at `--fs-label` or `--fs-xs`, or reaching it only through the receipt, either of which makes the pre-submit render carry no warning at the floor.
- **AC-6 — offline:** the setup disclosure's open/closed state survives a redraw. **Verified by:** a harness test asserting the rendered `<details class="next-cockpit-console-setup">` carries a `data-next-cockpit-disclosure` attribute whose key includes the route's project and focus, which is what `nextCockpitAfterRender` (next-cockpit.js:3360-3371) reads back; `nextCockpitConsoleStatus` already does this at :3092. **Falsified by:** emitting a bare `<details>`, which the cockpit's restore loop cannot see, so the section snaps shut on every poll.
- **AC-7 — offline, and assessable only once DRC-4590 has merged:** the promoted submit is styled with DRC-4590's shared non-primary control class, not as a primary action. **Verified by:** a `styles.css` assertion that the class DRC-4590 defines exists, and a harness assertion that the submit button carries it; on the tree measured today `grep -c 'next-action' styles.css` returns **0** and `grep -c -- '--radius' styles.css` returns **0**, and DRC-4590 is still at `status: triage` with no branch, so this criterion is **not yet assessable**. **Falsified by:** inventing a local `.next-action` rule in this change instead of consuming DRC-4590's, which forks the primitive the milestone exists to unify — if DRC-4590 has not merged when this is built, the submit keeps its current `.next-steer button` rule and `implementation` records the criterion as carried rather than met.
- **AC-8 — interactive:** with the Console panel scrolled to the top of the viewport, a reader sees DELEGATION, WAITING ON YOU, CAPACITY and TRIPWIRES before any setup prose, and the setup is one summary row. **Verified by:** a live drive repeating this triage's measurement — `python3 server.py --port 4597`, route `#n=project:<project>:<session>:console`, read `getBoundingClientRect().top` of `[data-next-project-rail]` relative to `[data-next-cockpit-panel="console"]` — and requiring the rail to start within 120px of the panel top with the disclosure shut, against **341px measured today** at 1680×1057. **Falsified by:** a rail offset that stays above 120px, which is what happens if the summary row is tall or if any setup section is left outside the disclosure other than the two AC-2 names. There is no offline substitute: this is a rendered pixel height, and the harness renders into a fake DOM with no layout.
- **AC-9 — interactive:** the `send` → `save draft` rename exists as a filed item against NUI-10 and is absent from this change. **Verified by:** the linked Linear issue, plus `grep -c 'save draft' cargento_runtime/web/next-controls.js` returning 0 on the merged branch. **Falsified by:** the rename shipping here, which would change the control NUI-10 keeps specifically to demonstrate the interaction shape, without the ruling that governs it being amended.

**Split:** seven offline (AC-1..AC-7), two interactive (AC-8, AC-9). AC-8 is the user-visible
property. No harness is proposed to automate AC-8: turning a fake-DOM harness into a layout engine
to assert one offset costs more than the criterion is worth, and DRC-4596 is already the milestone's
answer for making the asset test enforce size.

**Expected to scan as unevidenced at this gate.** `implementation` and `review` supply the `AC-N`
citations; the cross-check earns its keep at the review gate, not here.

## Expected surface

Measured against `spacedock-ensign/drc-4587` @ f6ced2f8. **Oracles costed separately from runtime,
because on this surface they are the larger half.**

**Runtime — 70 net lines across 4 files, tolerance ±25.**

| File | Change | Net lines |
|---|---|---|
| `next-cockpit.js` | reorder the console branch body (:3333-3341); wrap three sections in one `<details>` using `nextCockpitDisclosureAttr("console-setup")`; derive the summary from the two flags; hoist the `nextProjectSteer` call into `nextProjectCockpit` before `nextCockpitTabList()` | ~40 |
| `next-controls.js` | copy fix in `nextProjectSteer` (label + sentence replacing the mono header, new placeholder); bar-layout class on the promoted instance; **delete** the dead `nextProjectControls` (:171-175) | ~10 |
| `next-delegation.js` | drop the `true` at :291 | 1 |
| `styles.css` | `.next-cockpit-console-setup` summary; `.next-steer--bar`; the caveat sentence at `var(--fs-sentence)` with its own line-height | ~16 |

No new JS module, so `frontend_page.APP_PARTS` and its set-equality assertion
(`tests/test_next_page.py`) are untouched. Checked for a contract that would compel a new file —
`APP_PARTS` is the one that would, and it does not apply here.

**Oracles — 13 pinned figures across 3 test files. Recompute from the assets; never resolve one
textually.** These are the **post-DRC-4587** baselines, which are not the ones the recon quoted
from `main`:

| Pin | Site | Value on the 4587 tree | Value on `main` (stale) |
|---|---|---|---|
| `next-cockpit.js` | `test_next_page.py` | `198_105` / `16f67be9…` | `198_105` / `3c3fa0eb…` |
| `next-controls.js` | `test_next_page.py` | `18_071` / `ed2f27f3…` | same |
| `next-delegation.js` | `test_next_page.py` | `14_508` / `36ecd098…` | same |
| `styles.css` | `test_next_page.py` | `111_050` / `91a303b2…` | `108_008` / `ea8b79ed…` |
| assembled page | `test_next_page.py:710/713` | `914_344` / `2165bf68…` | `911_302` / `7c0dbcb3…` |
| assembled page | `test_next_flag.py:67/69` | `914_344` / `2165bf68…` | as above |
| assembled page | `test_focus.py:1024` | `2165bf68…` | as above |

All four parts move, so all five pairs move: 13 figures. Recomputing only `test_next_page.py`
leaves `test_next_flag.py` and `test_focus.py` red.

**Behavioural oracles that pin the arrangement being changed — 6 sites across 3 files, all of
which must move rather than be deleted:**

| Site | Asserts today | Why it moves |
|---|---|---|
| `test_next_delegation.py:186` | `DELEGATION` precedes `STEER · LOCAL ONLY` | the composer leaves the rail and moves ahead of the tab list; re-point to assert the composer is **not** within `data-next-guardrails` |
| `test_next_cockpit.py:1458`, `:1462` | `STEER · LOCAL ONLY` inside the console-panel slice | the composer renders outside that slice; re-point at the shell |
| `test_next_cockpit.py:1134` | `STEER · LOCAL ONLY` **absent** from a panel | still passes, but for a new reason — it becomes true by construction. Re-point or it silently stops measuring anything |
| `test_next_cockpit.py:3732`, `test_next_controls.py:486` | the literal `STEER · LOCAL ONLY` | the copy fix removes that string |

**Test files touched: 6** (`test_next_page`, `test_next_flag`, `test_focus`, `test_next_cockpit`,
`test_next_controls`, `test_next_delegation`), tolerance +1. No new test module is expected; if one
appears, that is the DRC-4037 overrun shape repeating and the estimating method is what should
change, not the tolerance.

**Docs:** `docs/design-next-ui.md` — NUI-10 gains the pre-submit warning and the promoted placement;
the Console-ordering ruling gets a paragraph. No test asserts NUI-10's copy today (checked), so this
is a review obligation rather than a compelled edit. Run `sync-docs` before the PR as usual.

**Semantics this change may move:** the composer's placement (panel → chrome, see the first gate
decision below) and the pre-submit copy. It may **not** move: the 500-character cap, the local-only
guarantee, the receipt text, or anything about what submitting does. NUI-10 is unamended except for
the pre-submit surface.

**Merge risk:** this PR owns `cargento_runtime/web/`, so no sibling PR may touch `web/`
concurrently. It is PR 6 of 6 in the milestone sequence, riding with DRC-4602 and DRC-4596.

## Decisions for the gate

**1. Does the promoted composer render on all five tabs, or only on Console?** The issue's own
solution says "from the cockpit shell immediately before the tab list", and its AC says "in the
chrome" — both mean **every tab**. Today it renders only on Console, because `nextProjectRail` has
exactly one call site. **Recommended: follow the issue — chrome, all five tabs.** The draft is
project-scoped and keyed by project label, so the project header is where it belongs, and AC-5's
sentence-tier warning is precisely the mitigation for giving a non-delivering control more
prominence. The alternative — make it the **first** child of the Console panel instead of the last
child of TRIPWIRES — also satisfies "lift the steer box out of TRIPWIRES", costs less (test
`test_next_cockpit.py:1134` stays as written), and keeps the control where a reader already expects
it. It cannot deliver the stated value, though: the issue's complaint is that the one writing
surface is subordinate to an unrelated section, and burying it one tab deep leaves it subordinate
to the tab strip instead. Flagging rather than deciding, because the acceptance criteria do not say
and the two readings produce different tests.

**2. `.next-action` does not exist, and DRC-4590 has not started.** `grep -c 'next-action'
styles.css` → **0**; DRC-4590 and DRC-4591 are both still `status: triage`, `linear-status:
Backlog`, with no branch. The PR sequence puts DRC-4590 in PR 2 and this in PR 6, so it should be
there in time — but the criterion must not assume it. AC-7 states the precondition and names the
fallback (keep `.next-steer button`, record the criterion as carried) rather than inventing a local
copy of the primitive, which would fork the thing the milestone exists to unify.

**3. Deleting `nextProjectControls` is in scope.** It is dead (no caller in `web/`) and it builds a
second composer, so AC-4's "renders once" is unprovable while it stands. Four lines. Declaring it
rather than doing it silently, because it is a deletion the issue did not ask for.

**Approach chosen, and the simplest rejected alternative.** Chosen: reorder the branch and wrap the
three setup sections in one persisted disclosure, reusing `nextProjectSteer` at a new call site.
Rejected: simply moving the three setup sections below the rail without a disclosure. It is smaller
— no `<details>`, no summary, no AC-3, no AC-6 — and it does deliver the ordering. It cannot
deliver the value, because the 309px does not go away: a reader who scrolls past DELEGATION still
meets three blocks of prose about switched-off capabilities before the panel ends, and the issue's
acceptance asks for "one summary row", not "the same prose, lower down".

## Stage Report: triage

- DONE: Capture the live Linear issue body and the owning milestone description verbatim under `## Linear edits made` as the pre-edit record before drafting anything, and draft the rewrite of each beside it without writing either to Linear.
  Both captures fenced verbatim from `get_issue DRC-4595` and `get_milestone "Clean and Cogent UI/UX"`; drafts sit beside them. Only read tools were called against Linear — no `save_issue`, no `save_milestone`.
- DONE: Write the acceptance criteria into `## Acceptance criteria` as bullets shaped `- **AC-1 — offline:** {property}. **Verified by:** {…}. **Falsified by:** {…}`
  Nine bullets, AC-1..AC-9, hyphenated, every bold run closing on its own line. Proved by running `status --read drc-4595 --ac-scan`, which resolves 9 — not by re-reading the file; a wrong shape returns `{"acs":[]}` rather than an error.
- DONE: Declare the expected surface with tolerance, costing the byte-pin oracles separately from the runtime, and measure every figure against the POST-DRC-4587 tree on branch `spacedock-ensign/drc-4587`, not against main
  Runtime 70 ±25 net lines / 4 files, tabled separately from 13 byte pins / 3 files and 6 behavioural sites / 3 files. Every pin read off the 4587 worktree: assembled is `914_344` / `2165bf68…`, **not** the `911_302` / `7c0dbcb3…` the recon carried from main; `styles.css` is `111_050`, not `108_008`.
- DONE: Confirm against the tree which of the controls this issue reorders actually exist after DRC-4590 lands ... State the dependency explicitly and do not write a criterion that assumes a primitive that is not yet merged.
  `grep -c 'next-action' styles.css` → 0 and `grep -c -- '--radius' styles.css` → 0 on the 4587 tree; DRC-4590 and DRC-4591 are both `status: triage`, `linear-status: Backlog`, no branch. AC-7 carries the precondition and a named fallback; gate decision 2 states it.

### Summary

The issue's structural claims all reproduce on the tree the work lands on; its quantitative claims
do not, so I drove the live board rather than re-reading the capture. Measured at 1680×1057 on
`spacedock-ensign/drc-4587` @ f6ced2f8: DELEGATION starts **341px into a 968px panel (35.2%)**, not
317px of 732px (43.3%), of which 309px is collapsible setup; the steer input sits **830px down**,
inside TRIPWIRES as filed. Three filed figures are now wrong and are dated into a history section
rather than deleted — the 10px mono label is 11px `--fs-label` since DRC-4587 collapsed that tier,
the sentence floor the warning is held to is 15px rather than 12.5px, and the interactive-element
counts came back 5/3/0/3 against the filed 5/0/0/1, so they are data-dependent and no criterion
rests on one. The rewrite is 680 words against 1,010.

Three things need the captain. **(1)** The issue's own wording puts the promoted composer in the
chrome, which renders it on all five tabs, not just Console — the acceptance criteria never say, and
the two readings produce different tests. Recommended: follow the issue. **(2)** `.next-action` does
not exist and DRC-4590 has not started, so AC-7 is written as not-yet-assessable with a named
fallback instead of assuming the primitive. **(3)** Deleting the dead `nextProjectControls` is in
scope, because AC-4's "renders once" is unprovable while a second construction path stands.
