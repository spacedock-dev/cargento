---
id:
title: 'A board claim cites the briefing for something the briefing does not say, and the citation carried a score demotion'
status: review
source: https://linear.app/recce/issue/DRC-4194
started: 2026-09-15T04:17:58Z
completed:
verdict:
score: 0.6
worktree:
issue:
pr: "#342"
mod-block:
linear-status: In Progress
milestone: Don't be the bottleneck
release: ''
estimate: 'XS'
reconciled:
move: none
promise: P3
gates:
    version: 1
    records:
        - id: gate:drc-4194:triage
          stage: triage
          attempts:
            - id: gate-attempt:drc-4194-triage-1
              briefing:
                id: briefing:drc-4194:triage:attempt-1:revision-1
                digest: sha256:94a8ad4ed4b97bc44b1d62c50d503a10072aa25d184a6b55a039c36f2ec3eb76
                room-ref: ./drc-4194/review/triage/briefing-1
              resolution:
                type: Resolution
                id: resolution:spacedock:drc-4194:triage:1
                briefing: briefing:drc-4194:triage:attempt-1:revision-1
                by: person:captain
                at: "2026-09-15T04:54:10.333686Z"
                decision: approve
                reason: 'Captain: I approve the pending queestions. Approves the presented triage draft, scope and proof boundary. Dispatch awaits the separately requested worker-reuse exception because fresh-agent capacity is exhausted.'
              application:
                target-stage: implementation
                state: consumed
---

[DRC-4194](https://linear.app/recce/issue/DRC-4194) — attribution correction; triage only.

## Linear edits made

No description, label or milestone was edited by this worker. FO moved the issue to In Progress. The following descriptions were fetched live on 2026-09-15 and are preserved verbatim.

### Original issue body — 2026-09-15

## User value

No Cargento user would notice this fix, since it only corrects an attribution inside the roadmap's own scoring audit trail rather than anything the dashboard shows. P3, none: fixing which document a demoted score actually cites changes no clause on the stopped-at-gate promise, because the claim never reached the product.

## What needs to be done

The claim "Pi has no passive needs-input signal at all" originates in B2's `note` and `inferred` fields at `docs/visibility-2x2/items.json:409`, tagged "(Pi, per the briefing)", and survives at `items.bak.json:427`. That `inferred` string is copied verbatim into `docs/visibility-2x2/audit/g2-detector.jsonl:11`, where the parenthetical reads as a source, and it is leaned on again at `docs/visibility-2x2/audit/attacks-2.jsonl:1` to justify a `"verdict":"demoted"`. `docs/visibility-2x2/briefing.md` does not contain the claim: its only needs-input sentence is `briefing.md:69`, which describes Cargento's own state badge rather than Pi's store, and its only mention of Pi is `briefing.md:28`, that Pi shows its own session in its own terminal. The claim is also overbroad: Pi publishes a lifecycle event model with a blockable `tool_call` and `agent_settled`, which contradicts "at all" even though a narrower reading is defensible.

Fix scope: correct the audit records so they cite where the claim actually comes from instead of the briefing, and either re-derive the demotion in `attacks-2.jsonl` from something real or mark it as resting on a self-citation. Do not rewrite `items.json`'s note itself; it is the dated record of what the panel scored and its `state` fields are deliberately stale. Grep `docs/visibility-2x2/audit/` for other "per the briefing" attributions while in there, since nothing checks them.

Acceptance criteria:

* The audit trail no longer attributes this claim to `briefing.md`.
* B2's demotion in `attacks-2.jsonl` is either re-derived from a real source or marked as resting on a self-citation.

### Original milestone description — 2026-09-15

## The user value

**One queue of everything blocked on you, with what it is waiting for and how long it has waited. A session can also ask you a question directly and wait for your answer.**

## What is left

[DRC-4379](<https://linear.app/recce/issue/DRC-4379>): In projects with the adapter installed, see OpenCode permission waits in the gate queue and clear them by answering in OpenCode.

[DRC-4380](<https://linear.app/recce/issue/DRC-4380>): See and clear extension UI prompts in persisted Pi sessions. The pair was measured on 0.85.1; stock project-trust startup and prompts before session persistence are outside this build.

[DRC-4208](<https://linear.app/recce/issue/DRC-4208>): Route Droid's hook events to the correct harness rather than silently losing them.

[DRC-4203](<https://linear.app/recce/issue/DRC-4203>): Keep a standing wait visible through ordinary heartbeats, with an explicit expiry.

[DRC-4200](<https://linear.app/recce/issue/DRC-4200>): Keep the captures supporting gate detection within their privacy contract.

[DRC-4199](<https://linear.app/recce/issue/DRC-4199>): Keep nearly simultaneous waits in a stable, truthful queue order.

[DRC-4194](<https://linear.app/recce/issue/DRC-4194>): Base the Pi gate-detection scoring audit on an actual source.

## Waits on

No open blocking relation is recorded on these issues.

## One question left open, gating nothing

Whether a raise survives the terminal that launched it. Cargento ships the socket case alone, so nothing that shipped depends on the answer. The instrument that would settle it works and has not been run.

## Problem

The source attribution defect is present on main `3c4abe2`. The dated detector audit repeats B2's own Pi no-signal assumption as if the briefing established it; the second attack then uses the note itself as support for a demotion proposal. Correct that provenance without replacing the historical panel judgments or changing a score.

Fresh Linear read: In Progress, Medium priority, XS, `move:none` and `journey:stopped-at-gate`; no release label, no blockedBy/blocks, related DRC-4190 and DRC-4014, and zero comments with hasNextPage=false. No Design reference. Owning milestone freshly includes the approved OpenCode and Pi boundary wording. Project overview has no literal “As of” block; read current What is next and Open decisions instead. DRC-4379's selection report supplies full membership and ranking; its seven-open selection is dated, not a fresh status census here.

## Evidence and stale assumptions

| Source on main 3c4abe2 | Actual evidence |
|---|---|
| `docs/visibility-2x2/briefing.md:28–31` | Pi belongs to a group whose own session is visible in its own terminal. No statement about absence of passive Pi events appears. |
| `briefing.md:69–73` | “needs-input for Claude only” describes what Cargento already derives, not all signals Pi could expose. |
| `items.json` B2.note and B2.inferred; same fields in items.bak.json | The note asserts no passive Pi signal; inferred carries “Pi, per the briefing”. The strings are the local panel record, not independent native-source evidence. No chronology between these fields and the audit is inferred from matching strings. |
| `audit/g2-detector.jsonl:11` B2 | inferred equals items.json B2.inferred, including the unsupported attribution; riskDiscount=22. |
| `audit/attacks-2.jsonl:1` B2 | impactAttack says “The note itself concedes Pi has no passive needs-input signal at all”; proposedAccess=58, proposedImpact=72, verdict=demoted, strength=moderate. |
| `audit/verify-scores.py:32–47,69–83` | Reads blind scores, g2-kinds, strong g2-boundary findings and detector riskDiscount. It never reads attacks-2. Textual inferred/impactAttack are not numeric inputs. |
| Actual B2 blind scores | A:58/82, B:68/76, C:72/82 (access/impact); medians are 68/82. Current items.json keeps 68/82 and 82−22=60 risk-adjusted impact. |

Bounded command `python3 docs/visibility-2x2/audit/verify-scores.py` exited 0: 171 blind scores over 57 items, all 57 match. This proves existing numerical derivation consistency, not whether the audit's prose is supported. It suppresses JSONDecodeError while reading evidence, so explicit line-by-line JSON parsing remains a necessary implementation check.

The issue title's “carried a score demotion” must be narrowed: the record contains a demotion proposal, but that proposal is not an applied override in the current scorer. Do not claim historical numerical causation without evidence. The detector discount is a separate 22-point input and remains frozen in this issue.

The original issue's suggestion that tool_call and agent_settled themselves contradict “no passive needs-input signal at all” is not needed to establish this defect; a blockable tool event or a settled event alone does not prove a standing UI prompt signal. The separate DRC-4380 triage measured the actual ui_prompt pair on Pi 0.85.1. This historical correction neither retroactively imports today's native events into the panel nor reruns that adapter investigation.

Exact case-insensitive “per the briefing” scan returned three audit rows:
- g2-detector B2: unsupported Pi no-signal attribution, the subject of this issue.
- scores-C-3 A2: /usage plus Barista cached weekly quota/reset visibility is stated in the briefing's Claude paragraph. Leave unchanged.
- g2-scores-C-1 C4: no cross-session originating-goal view is consistent with the briefing's cross-cutting “No harness displays anything about sessions other than the one in front of you.” Leave unchanged; this validates the attributed clause, not every other C4 sentence.

No broad search claim is made about all uses of “briefing”, all native capability assertions or all scoring rationales. The three source documents first landed together in `56a48a4` (2026-07-31, #68); the repository log alone does not establish which authoring step produced the Pi assertion first.

## Before-development calibration

Applied burndown deep-dive through proposal and visual-review-and-fix Mode 1 calibration. `move:none` is appropriate: no Cargento user sees changed behavior or text; only two internal audit prose fields change. P3 is the historical subject, not a promise improvement.

Mode 1 selects **skip: prose with no claim about the board**. The shipped dashboard does not load these audit files. The prioritization board's serve.py re-reads items.json for /api/items, and its index.html fetches that endpoint; items.json stays untouched. A browser would therefore show the same stored panel record and cannot establish the provenance correction. Source-to-source comparison and actual score recomputation are the instruments for this class. No browser was requested, no server started, no screenshot claimed, and no full suite was run during triage.

## Proposed approach

Edit exactly two existing JSONL string fields, with dated clarification inside their prose; no schema additions:
1. B2.inferred in g2-detector: identify the paragraph as historical panel reasoning, replace the “Pi, per the briefing” attribution with “Pi: assumption recorded in items.json B2.note/B2.inferred; not established by briefing.md”, and make clear that this names the assertion's location rather than an independent source proving it.
2. B2.impactAttack in attacks-2: replace the “note itself concedes” sentence with a dated clarification that the Pi premise is a self-citation to that same panel note, not independent evidence; the demoted verdict is retained as the historical proposal and is not a current numerical override.

Retain the remaining reasoning and all original scalar scores/verdicts, preserving the rest of each record. The git diff retains the precise original wording; the dated clarification says why the provenance changed. Do not update the note in items.json or either panel snapshot.

Rejected alternative: re-derive the demotion from today's Pi API. It would answer a different, version-dependent question and reopen panel scoring. The original issue explicitly permits marking self-citation, which is the smaller correction substantiated by the files in hand. No new product decision is necessary for that permitted branch; any proposal to change riskDiscount or current scores returns to the gate.

## Draft issue rewrite — not published

Proposed title: **Correct B2's unsupported Pi attribution in the historical scoring audit**.

Publication assembly (not issue text): publish the User value and Correction sections below, then the complete AC-1 through AC-5 bullets and the interactive-none sentence under this entity's canonical Acceptance criteria section as the issue's Acceptance criteria. Finally append the complete verbatim original issue body above under **Historical context — captured 2026-09-15** with the short historical note below. This is an exact, self-contained issue assembly; do not publish assembly instructions, milestone draft, budgets or report.

### User value

No Cargento user sees this correction: it changes only the roadmap's historical audit attribution, leaving the dashboard and saved panel scores unchanged. P3, none: identify which panel assumption an old Pi demotion proposal reused.

### Correction

Correct only B2.inferred in `docs/visibility-2x2/audit/g2-detector.jsonl` and B2.impactAttack in `docs/visibility-2x2/audit/attacks-2.jsonl`. Date the clarification: the no-passive-Pi-signal premise is recorded in `items.json` B2.note/B2.inferred and is not established by `briefing.md`; repeating the note is a self-citation, not independent evidence.

Keep the demoted verdict as a historical proposal, with its self-citation stated. Do not say it altered current applied scores: verify-scores.py ignores attacks-2, and B2's current 68 access/82 impact are panel medians. Preserve all numerical fields, every other audit row, the briefing, items.json and items.bak.json.

The bounded exact-phrase audit also found A2 and C4 citations that point to actual briefing statements; leave them unchanged. No new audit framework, scorer, test file, native-signal investigation or panel rerun.

### Historical note for the assembled issue

The original wording below is history. Its attribution problem survives; its implied applied demotion and native-event argument are not current instructions.

## Draft milestone correction — not published

Replace only the DRC-4194 What is left line with:

[DRC-4194](https://linear.app/recce/issue/DRC-4194): Correct the Pi attribution in the historical scoring audit and mark its demotion proposal as relying on a self-citation, without changing panel scores.

The current “Base ... on an actual source” risks implying a new independent native source or re-score; the replacement states the permitted self-citation outcome. Preserve all sibling lines and re-fetch the milestone before applying this one-line edit after approval.

Labels: retain `journey:stopped-at-gate` and `move:none`; retain no release label rather than silently assign priority. The missing release label is the known selection gap. XS remains appropriate. No direct user-visible criterion is owed because the User value explicitly identifies the move:none boundary.

## Acceptance criteria

- **AC-1** — offline: B2's detector audit identifies its Pi no-signal premise as an assumption recorded in `items.json` B2.note/B2.inferred, not a fact established by `briefing.md`. **Verified by:** parse B2 from g2-detector.jsonl and compare its provenance statement with the unchanged panel fields and the complete briefing; falsified by still presenting “per the briefing” as support, or by treating the note as independent native evidence.
- **AC-2** — offline: B2's attacks-2 impact argument explicitly marks the Pi premise as a self-citation and the demotion as a historical proposal without independent support for that premise. It does not claim that the proposal changed the current applied scores. **Verified by:** compare the amended B2 impactAttack with the unchanged original note and inspect verify-scores.py's actual inputs; falsified by “the note concedes” being used as independent proof or an assertion of an applied 58/72 override.
- **AC-3** — offline: The only audit changes are B2.inferred in g2-detector.jsonl and B2.impactAttack in attacks-2.jsonl; their historical numeric values, verdict/strength, every other field and every non-B2 record remain unchanged. `items.json`, `items.bak.json`, the briefing and all score files remain byte-identical to the implementation merge base. **Verified by:** JSON-aware before/after comparison allowing precisely those two scalar fields, plus git diff and file hashes; falsified by any additional changed field or frozen-file byte.
- **AC-4** — offline: Audit JSONL remains parseable and existing score derivation still passes without a new scorer, test file or panel run. **Verified by:** parse every nonempty line in both edited files (the scorer suppresses JSON decode errors, so its success alone is insufficient), run `python3 docs/visibility-2x2/audit/verify-scores.py`, and complete canonical pre-PR checks with sync-docs; falsified by malformed JSONL, a score mismatch or modified oracle used to accept one.
- **AC-5** — offline: The bounded exact-phrase inventory is accounted for: B2 is corrected; A2 and C4's other “per the briefing” attributions stay unchanged because their cited clauses are present. **Verified by:** `rg -n -i 'per the briefing' docs/visibility-2x2/audit` and direct comparison to briefing's Claude paragraph and cross-cutting cross-session statement; falsified by claiming a corpus-wide attribution audit or rewriting unrelated panel rationale.

Interactive criteria: none. This move:none correction changes internal historical audit attribution, not a runtime or board-rendered property. No browser or panel rerun is needed.

## Expected surface and tolerance

| Category | Expected | Tolerance and trigger |
|---|---|---|
| Runtime | 0 files, 0 lines | No runtime, web, collector, schema or configuration edits authorized. |
| Tests and oracles | 0 changed files, 0 new tests | Existing scorer and explicit JSON/diff checks suffice. No new scoring or provenance checker. |
| Historical audit prose | Exactly 2 JSONL files, one existing B2 scalar string each; 2 physical replaced lines, about 60–140 words of clarification | Up to 40 additional clarification words; no third file or additional field without returning to the gate. |
| Tracker drafts/state | This entity plus approved issue and one milestone line during implementation | No sibling changes or full milestone replacement. |
| Documentation reconciliation | sync-docs review; expected 0 additional repository docs | Unexpected owner-doc need must be named, not silently widen the two-file correction. |

No existing check compels a new test file: no new runtime module for test_contracts' import graph, no adapter for runtime inventory/protocol fakes, and no added published field. Source-reference search found no test reading the two audit files' prose. The existing score checker consumes detector riskDiscount and leaves the proposed text fields irrelevant to arithmetic. Canonical pre-PR local checks are still required for the eventual PR, even if CI's prose path skips measurable jobs.

Calibrated implementation estimate: 20–40 minutes including bounded verification and ordinary local checks, plus shared-suite/CI scheduling. Uniform multi-lens treatment would cost roughly 1–2 hours and is not warranted here. Triaging a textual provenance correction does not authorize a full test suite competing with the active adapter workers.

## Test plan

Implementation owns the following checks, coordinated with FO:
- JSON-load both edited files line by line, reject malformed lines, and compare pre/post objects: only the two named B2 fields may differ.
- Compare items.json, items.bak.json, briefing and score-file bytes against the implementation merge base. Main-triage SHA-256 for items.json: `d88faab0189c6751e3cd1825aab9e6d4eb5665ad195360f7b709d412a8f84419`; items.bak.json: `e5c0dc81c85ff609473bcb6aaef637b87e63fe2fd74ff6aa618a5f8b5fd346d0`. Recompute after any rebase; do not mistake a sibling change for this patch.
- Run existing verify-scores.py, inspect the actual cited briefing clauses, then run canonical pre-PR checks and sync-docs without adding standing tests.
- Retain the exact-phrase inventory result and explicit scope limit in the delivery receipt.

## Review depth

Self-verify under AGENTS.md's “No user-visible behaviour change and nothing calls it yet” row: neither edited prose field has a runtime consumer, no score input changes, and there is no security/data-loss surface. Read the two-record diff and substantiate the attribution against source; an adversarial panel rerun would be the wrong instrument. No delegates were used.

## Out of scope

Native Pi adapter work or new captures; changing any panel snapshot, score, risk discount, applied verdict, source briefing, other audit row or project promise; broad attribution sweeps; a new auditor/scorer; browser work; runtime fixes; PR creation or Linear description writes during triage. No process cleanup is owed because none was started.

## Stage Report: triage

- DONE: Establish the real issue and relevant contract from fresh tracker/source evidence, with before-development calibration and stale assumptions explicit.
  Live issue/comments/relations/milestone/project read; full briefing and exact audit records compared; existing scorer executed successfully; Mode 1 calibrated skip for unrendered audit prose.
- DONE: Preserve original records and draft precise user-value issue/milestone corrections plus independently verifiable acceptance criteria and scope decisions.
  Originals copied verbatim above; self-contained issue assembly and minimal milestone correction drafted; canonical AC-1–AC-5 identify source, object-diff, parse and score evidence.
- DONE: Commit bounded triage evidence, smallest approach, separate surface budgets, tolerance and review depth without implementing or publishing drafts.
  Entity-only report; two-field prose repair proposed, runtime/test budgets zero, self-review selected; no implementation, gate approval or Linear body write.
- SKIPPED: Browser walk and full suite during triage.
  No rendered surface changes; the bounded scorer is appropriate, and shared suites remain implementation work coordinated by FO.

### Summary

The detector's briefing attribution is unsupported, and the attack repeats the panel's own note. Its demotion is a historical proposal rather than an applied current override; the existing scorer confirms the panel-derived 68/82 and separate 22-point discount. Correct two audit prose fields and disclose self-citation, preserving every panel number and snapshot.

## Stage Report: implementation

- DONE: Correct B2.inferred in docs/visibility-2x2/audit/g2-detector.jsonl.
  Replaced "(Pi, per the briefing)" with "(Pi: assumption recorded in items.json B2.note/B2.inferred; not established by briefing.md)".
- DONE: Correct B2.impactAttack in docs/visibility-2x2/audit/attacks-2.jsonl.
  Clarified that the Pi premise is a self-citation to items.json B2.note rather than independent evidence, and that the demoted verdict is a historical proposal, not an applied score override.
- DONE: Verify JSON validity, score calculation, and pre-PR suite.
  Verified line-by-line JSON parseability, verify-scores.py (all 57 items match), full unit test suite, and pre-PR linters/validators. Opened PR #342 with signed commit.
